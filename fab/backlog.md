## Backlog

- [ ] [38a6] 2026-06-13: Bake HOMEBREW_NO_REQUIRE_TAP_TRUST=1 into shll's brew install/upgrade invocations as a temporary workaround for a Homebrew 6.0 Linux-sandbox bug. ROOT CAUSE (diagnosed 2026-06-12 on idea v0.0.9->0.0.10 upgrade; not a shll or tap bug): Homebrew 6.0's Linux bubblewrap sandbox runs the formula build/install in build.rb inside bwrap, where deny_read_home (Library/Homebrew/sandbox.rb ~line 191) masks almost all of $HOME. Its exception list covers HOMEBREW_PREFIX/CACHE/LOGS/TEMP but NOT HOMEBREW_USER_CONFIG_HOME (~/.homebrew), where trust.json lives. So when HOMEBREW_REQUIRE_TAP_TRUST=1 is set, the sandboxed build.rb re-checks tap trust, cannot read ~/.homebrew/trust.json, and raises Homebrew::UntrustedTapError at build.rb:264 — three lines BEFORE the error pipe is connected (build.rb:267), so the message is swallowed and brew surfaces only an opaque 'bwrap ... exited with 1' with a giant arg dump. This breaks 'brew install/upgrade sahil87/tap/<formula>' for EVERY roster tool on any Linux box where the user set HOMEBREW_REQUIRE_TAP_TRUST=1 — which shll itself encourages via 'shll shell-setup --trust-tap' (writes that export line). Verified workaround: prefix the brew call with HOMEBREW_NO_REQUIRE_TAP_TRUST=1 (keeps the sandbox active, skips only the broken in-sandbox trust re-check); 'HOMEBREW_NO_REQUIRE_TAP_TRUST=1 brew upgrade sahil87/tap/idea' completed cleanly. IMPLEMENTATION: inject HOMEBREW_NO_REQUIRE_TAP_TRUST=1 into the environment of shll's brew-spawning subprocesses ONLY (the brew install/upgrade/update calls), not per-tool '<tool> update' delegations or unrelated procs. Call sites are all in internal/proc via exec.CommandContext (proc.go) — Request has NO Env field today (cmd.Env is never set, so children inherit the parent env). Add an 'Env []string' (or map) field to proc.Request and have defaultRunner append it to cmd.Environ(); then set it on the brew install/upgrade/update Requests. The brew foreground calls live at: install.go:145 (brew install <formula>), update.go:242 (brew update --quiet), update.go:295 (brew upgrade shll self), and update.go:417/upgradeArgv -> upgradeTool at update.go:404 (brew upgrade <formula> fallback). Centralize the env injection near brewBinary in brew.go (e.g. a brewEnv() helper) so all brew calls share one source of truth and the follow-up removal is a one-spot edit. DESIGN TENSION TO RESOLVE (do not ignore): shll is deliberately PRO-trust — shell-setup --trust-tap runs 'brew trust --tap sahil87/tap' and writes 'export HOMEBREW_REQUIRE_TAP_TRUST=1', and brew.go's trustHatchHint already advertises HOMEBREW_NO_REQUIRE_TAP_TRUST=1 as an escape hatch. Setting HOMEBREW_NO_REQUIRE_TAP_TRUST=1 unconditionally on every brew call would silently neuter that trust enforcement during install/upgrade even on platforms (macOS) where the bug does not exist. PREFERRED scoping: gate the workaround to Linux only (runtime.GOOS == 'linux'), since the sandbox bug is Linux-bubblewrap-specific — macOS sandboxing is unaffected and should keep enforcing trust. Document the override loudly in code comments AND link it to the removal backlog item so it is not mistaken for permanent design. Add/extend a proc or brew unit test asserting the brew Requests carry HOMEBREW_NO_REQUIRE_TAP_TRUST=1 (on linux) and do NOT on darwin. Update spec/memory noting this is a temporary brew-bug workaround, cross-referencing the removal item.
- [ ] [tkch] 2026-06-13: Remove the HOMEBREW_NO_REQUIRE_TAP_TRUST=1 workaround once the upstream Homebrew Linux-sandbox bug is fixed. This is the cleanup follow-up to backlog item [38a6] (which baked the override into shll's brew install/upgrade/update calls). BACKGROUND: Homebrew 6.0's Linux bwrap sandbox masks ~/.homebrew (HOMEBREW_USER_CONFIG_HOME) via deny_read_home, so the sandboxed build.rb cannot read trust.json and wrongly raises UntrustedTapError when HOMEBREW_REQUIRE_TAP_TRUST=1 is set; the error is also swallowed (raised before build.rb connects its error pipe), surfacing as an opaque 'bwrap exited with 1'. shll injects HOMEBREW_NO_REQUIRE_TAP_TRUST=1 on its brew calls to keep installs working without disabling the sandbox. Two distinct upstream bugs feed this; EITHER being fixed may suffice, but verify before removing: (1) deny_read_home in Homebrew/brew Library/Homebrew/sandbox.rb should add an exception for HOMEBREW_USER_CONFIG_HOME (~/.homebrew) so the in-sandbox trust re-check can read trust.json; (2) build.rb should connect the error pipe BEFORE parsing args / running the trust check so early exceptions are not silently dropped. HOW TO VERIFY THE FIX LANDED (do this before deleting the workaround): on a Linux box with HOMEBREW_REQUIRE_TAP_TRUST=1 set and the tap trusted (~/.homebrew/trust.json lists sahil87/tap), run a clean 'brew upgrade sahil87/tap/idea' (or any roster tool) WITHOUT HOMEBREW_NO_REQUIRE_TAP_TRUST set, on the current Homebrew. If it completes without the bwrap/UntrustedTapError failure, the upstream fix is in. Track upstream at github.com/Homebrew/brew (search issues/PRs for sandbox deny_read_home HOMEBREW_USER_CONFIG_HOME / build.rb error pipe trust); if no issue exists, one should be filed (the diagnosis from change-of-2026-06-12 has the full repro). REMOVAL: revert the [38a6] changes — drop the HOMEBREW_NO_REQUIRE_TAP_TRUST=1 injection (the brewEnv()/Env-field wiring in proc.go + brew.go and its use at the install/update call sites), remove the now-stale code comments and the linux-gating, and delete the workaround unit tests (or flip them to assert NO override is set). If [38a6] added an 'Env' field to proc.Request that nothing else uses after removal, consider whether to keep it (harmless, generally useful) or strip it. Update spec/memory to drop the temporary-workaround note. Cross-ref: pairs with [38a6].
- [ ] [std1] 2026-07-18: Toolkit-standards rollout, wave 1 — dispatch "Directive 1 — Constitution amendment" (full prose in the "Toolkit standards rollout directives" section below) to the tool repos `hop`, `wt`, `tu`, `idea`, `run-kit`, `fab-kit` (optionally `shll` itself — same amendment). All repos in parallel; no cross-repo dependency. Each repo lands a docs-type fab change amending `fab/project/constitution.md` with the Toolkit Standards article. PREREQ: none (the article references only the `shll standards` command and the shll repo's docs/site tree, so it survives standards additions/renames and the docs/site/standards/ move by design).
- [ ] [std2] 2026-07-18: Toolkit-standards rollout, wave 2 — per repo, once THAT repo's [std1] PR is merged, dispatch "Directive 2 — Conformance change" (full prose below) to the same repos. PREREQ (hold until true): the shll release carrying the `standards` subcommand AND the docs/site/standards/ directory move is published and installed on worker machines (`shll standards` runs clean; `shll standards --json` source_paths read `docs/site/standards/…`). Deliverable per repo: one fab change with fixes + a per-standard conformance report in the PR body (PASS / fixed-here / deferred-with-ref), citing the audited shll version.
- [ ] [agst] 2026-07-18: Add `shll agent-setup` + `shll skill` — graduate machine-level agent wiring from run-kit to shll. NAMING: the per-tool command is `<tool> skill` (NOT `<tool> agent` — `fab agent` already exists meaning "launch an agent session"; run-kit's `agent-*` family means harness wiring). DESIGN: (1) `shll skill` composes per-tool agent skill bundles the way `shell-init` composes shell-init — at runtime, by invoking each installed tool's own `<tool> skill` subprocess (Constitution III; bundles stay version-locked to the INSTALLED tool, unlike `shll standards` which embeds shll's own docs). Grammar deliberately mirrors `shll standards`: bare form = one line per tool (cheap glossary; shll-first row ordering like list/version); `shll skill <tool>` = that tool's full bundle. Do NOT make the bare form concatenate all bundles — 7×~150-line bundles into agent context violates principle №9; the two-step (list, then per-tool on demand) is the context-economy contract. Capability-probed graceful degradation per Constitution V: a tool that is missing or predates `<tool> skill` is skipped silently (probe like the --skip-brew-update pattern), so this can ship before all 7 tools implement `skill`. (2) `shll agent-setup` wires system-installed agent harnesses: detect installed harness context files (Claude Code ~/.claude/CLAUDE.md; Codex/Cursor/OpenCode AGENTS.md-family locations — anc.dev precedent ships skill bundles for exactly these four harnesses) and write an idempotent sentinel-wrapped toolkit stanza into each — reuse `shell-setup`'s sentinel-block machinery (--print / --uninstall / show-diff-and-confirm, matching run-kit agent-setup's existing UX). The stanza teaches the two-step: run `shll skill` to see the toolkit's tools + one-liners, `shll skill <tool>` before driving a tool; plus one trailing line noting `shll standards` exists for toolkit-repo development (repo-level enforcement stays in each repo's constitution via [std1] — keep the global stanza lean). Then DELEGATE to `run-kit agent-setup` for run-kit's harness hooks (Constitution III/IV: compose, don't absorb; run-kit agent-setup keeps working standalone). (3) COORDINATED run-kit change, land BEFORE or WITH this one to avoid double-injection: slim `run-kit agent-setup` to hooks-only — its context-injection part (the stanza pointing agents at `run-kit context`) is removed; that guidance moves into run-kit's own `run-kit skill` bundle, so agents still learn `run-kit context` via the composed path. (4) Graduate the two touchpoints that currently say `run-kit agent-setup`: shll install's post-install "Next steps" nudge (change 93r2) and the README install flow — both become `shll agent-setup`. (5) Constitution VII justifications: `skill` = composition of per-tool commands (the shell-init precedent); `agent-setup` = cross-toolkit machine wiring graduating from run-kit where it was mis-homed. DEPS: the skill standard must be authored first (docs/site/standards/skill.md — defines the `<tool> skill` genre, length budget ~150 lines, canonical docs/site/skill.md source + embed/drift-guard pattern per the vo8c mechanism) and at least run-kit's `<tool> skill` implemented (others degrade gracefully); per-tool `<tool> skill` implementations ride the per-repo waves after [std1]/[std2]. Sequence: skill standard (shll) → run-kit skill + agent-setup slim (run-kit) → this change (shll) → remaining tools' skill commands land whenever, picked up automatically.

## Toolkit standards rollout directives (std1 / std2)

Operator brief: Repos: `hop`, `wt`, `tu`, `idea`, `run-kit`, `fab-kit` (optionally `shll` itself — same amendment applies). Dispatch Directive 1 to all repos in parallel; there is no cross-repo dependency. Directive 2 dispatches per repo as soon as that repo's Directive-1 PR is merged — no need to wait for the full wave. Precondition for Directive 2 anywhere: a shll release carrying the `standards` subcommand (post docs/site/standards/ move) is published and installed on the worker machines (`shll standards` runs clean); if not, hold Directive 2.

### Directive 1 — Constitution amendment: toolkit standards conformance (paste per repo)

**Task: Amend this repo's fab constitution to bind it to the sahil87 toolkit standards.**

This repo is part of the sahil87 toolkit. The toolkit publishes binding, producer-facing standards — CLI design principles plus mechanical contracts (machine-readable help output, README/docs-site structure, and others over time). They are canonically authored in the `sahil87/shll` repository's `docs/site/` tree, rendered on https://shll.ai, and readable offline via the `shll standards` command. This change adds a constitution article so every future pipeline run in this repo loads and enforces the obligation.

Make this change:

1. In `fab/project/constitution.md`, add a new article under **Additional Constraints** (create the section if this constitution lacks it, matching the file's existing structure):

   > ### Toolkit Standards
   > This tool is part of the sahil87 toolkit and MUST conform to the toolkit's published standards. The standards are enumerated by running `shll standards` — each entry names what it governs; read one with `shll standards <name>`. Before changing the CLI surface, help output, `README.md`, or `docs/site/`, the change MUST be checked against the standards governing that surface. If `shll` is unavailable, the canonical sources are the `sahil87/shll` repository's `docs/site/` tree (rendered on https://shll.ai). Standards added or revised there bind this repo without further amendment to this constitution.

2. Bump the constitution's `Last Amended` date (and version, per this file's own governance line).
3. Deliberate constraint: do NOT copy standard names, counts, or per-standard URLs into the constitution — `shll standards` is the enumeration, and the article must stay correct as standards evolve.

Ship per this repo's normal flow (docs-type fab change → PR). Nothing else is in scope — no conformance fixes in this change.

### Directive 2 — Conformance change (paste per repo, only after that repo's Directive-1 PR is merged)

**Task: Bring this repo and its tool into conformance with the sahil87 toolkit standards.**

Precondition: `shll standards` runs on this machine (if the subcommand is missing, run `shll update`; if it still fails, stop and report — do not proceed from memory or the website). This repo's constitution carries the Toolkit Standards article; this task is the conformance work it mandates.

1. **Enumerate at runtime**: run `shll standards`, then `shll standards <name>` for every listed entry. The list is authoritative — do not assume which standards exist or what they require.
2. **Audit** this repo against each standard. For mechanical contracts (machine help output, README/docs-site structure), execute the standard's own verification checklist verbatim. For the principles, assess each numbered principle against the tool's actual behavior — prompts and TTY handling, stdout/stderr separation, `--json`/`--dry-run`/`--yes` coverage, exit codes and error wording, idempotency, output volume.
3. **Fix what is proportionate here**: all mechanical-contract violations, and principle gaps that are small and additive (a missing flag, a misrouted stream, an unhelpful error). Larger gaps that would restructure the tool are NOT for this change — record each as a draft change or issue per this repo's convention and reference it.
4. **Deliverable**: one fab change whose PR body contains a conformance report — one section per standard with PASS or the gaps found, each gap dispositioned as *fixed here* (with the commit) or *deferred to <ref>*. Include the shll version audited against (`shll version`'s shll row), since standards are versioned with the shll release. Tests green; if the command tree changed, re-verify the machine-help contract afterward.
