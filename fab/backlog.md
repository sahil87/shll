## Backlog

- [x] [38a6] 2026-06-13: Bake HOMEBREW_NO_REQUIRE_TAP_TRUST=1 into shll's brew install/upgrade invocations as a temporary workaround for a Homebrew 6.0 Linux-sandbox bug. ROOT CAUSE (diagnosed 2026-06-12 on idea v0.0.9->0.0.10 upgrade; not a shll or tap bug): Homebrew 6.0's Linux bubblewrap sandbox runs the formula build/install in build.rb inside bwrap, where deny_read_home (Library/Homebrew/sandbox.rb ~line 191) masks almost all of $HOME. Its exception list covers HOMEBREW_PREFIX/CACHE/LOGS/TEMP but NOT HOMEBREW_USER_CONFIG_HOME (~/.homebrew), where trust.json lives. So when HOMEBREW_REQUIRE_TAP_TRUST=1 is set, the sandboxed build.rb re-checks tap trust, cannot read ~/.homebrew/trust.json, and raises Homebrew::UntrustedTapError at build.rb:264 — three lines BEFORE the error pipe is connected (build.rb:267), so the message is swallowed and brew surfaces only an opaque 'bwrap ... exited with 1' with a giant arg dump. This breaks 'brew install/upgrade sahil87/tap/<formula>' for EVERY roster tool on any Linux box where the user set HOMEBREW_REQUIRE_TAP_TRUST=1 — which shll itself encourages via 'shll shell-setup --trust-tap' (writes that export line). Verified workaround: prefix the brew call with HOMEBREW_NO_REQUIRE_TAP_TRUST=1 (keeps the sandbox active, skips only the broken in-sandbox trust re-check); 'HOMEBREW_NO_REQUIRE_TAP_TRUST=1 brew upgrade sahil87/tap/idea' completed cleanly. IMPLEMENTATION: inject HOMEBREW_NO_REQUIRE_TAP_TRUST=1 into the environment of shll's brew-spawning subprocesses ONLY (the brew install/upgrade/update calls), not per-tool '<tool> update' delegations or unrelated procs. Call sites are all in internal/proc via exec.CommandContext (proc.go) — Request has NO Env field today (cmd.Env is never set, so children inherit the parent env). Add an 'Env []string' (or map) field to proc.Request and have defaultRunner append it to cmd.Environ(); then set it on the brew install/upgrade/update Requests. The brew foreground calls live at: install.go:145 (brew install <formula>), update.go:242 (brew update --quiet), update.go:295 (brew upgrade shll self), and update.go:417/upgradeArgv -> upgradeTool at update.go:404 (brew upgrade <formula> fallback). Centralize the env injection near brewBinary in brew.go (e.g. a brewEnv() helper) so all brew calls share one source of truth and the follow-up removal is a one-spot edit. DESIGN TENSION TO RESOLVE (do not ignore): shll is deliberately PRO-trust — shell-setup --trust-tap runs 'brew trust --tap sahil87/tap' and writes 'export HOMEBREW_REQUIRE_TAP_TRUST=1', and brew.go's trustHatchHint already advertises HOMEBREW_NO_REQUIRE_TAP_TRUST=1 as an escape hatch. Setting HOMEBREW_NO_REQUIRE_TAP_TRUST=1 unconditionally on every brew call would silently neuter that trust enforcement during install/upgrade even on platforms (macOS) where the bug does not exist. PREFERRED scoping: gate the workaround to Linux only (runtime.GOOS == 'linux'), since the sandbox bug is Linux-bubblewrap-specific — macOS sandboxing is unaffected and should keep enforcing trust. Document the override loudly in code comments AND link it to the removal backlog item so it is not mistaken for permanent design. Add/extend a proc or brew unit test asserting the brew Requests carry HOMEBREW_NO_REQUIRE_TAP_TRUST=1 (on linux) and do NOT on darwin. Update spec/memory noting this is a temporary brew-bug workaround, cross-referencing the removal item.
- [x] [pv7t] 2026-07-18: Add proactive-capability trigger vocabulary (run-kit visual display + notify) to the placed shll-toolkit Agent Skill description and the `shll skill` roster line. PROBLEM: run-kit has two agent-PROACTIVE capabilities — visual display (iframe windows + /proxy + the Visual Display Recipe; topic page `rk skill display`, run-kit PR #386) and out-of-band notify (`rk notify`) — that an agent should reach for UNPROMPTED when it has visual content to show or needs to ping the human. Today they are invisible at every ambient layer: the placed SKILL.md description (built by agentSkillDescription() in src/cmd/shll/agent_setup.go from Roster.SkillHint) renders run-kit as "tmux sessions (run-kit/rk)" (SkillHint at src/cmd/shll/tools.go:155), so a display-shaped request ("show me this as a diagram/report in the browser") matches nothing; the hop-1 roster line (Roster.Description: "tmux session manager with a web UI") also lacks display/notify vocabulary; the FIRST mention of "show web content visually" is hop-2 (`shll skill run-kit` — the bundle's own "When to use" section). The agent-setup design already names the governing principle (agent_setup.go comment: "the description is the only text in an agent's context BEFORE the skill is invoked, so task-shaped requests must match there, not just tool names") — and since change agst deliberately REJECTED bundle aggregation/per-tool skill placement in favor of the thin bootstrap + runtime two-step (docs/memory/cli/standards-content.md §forward design), the description IS the designed ambient trigger surface: this is a missing-vocabulary bug in it, not a missing mechanism. DISTINCTION WITH TEETH (sprawl guard): reactive tools (wt/idea/tu/hop/fab-kit) live correctly behind the two-step router because the USER'S words name them; only agent-proactive capabilities earn description space, and today that set is exactly run-kit's display + notify. IMPLEMENTATION (preferred): add an optional Roster field (e.g. ProactiveHint string, empty for most tools) rendered by agentSkillDescription() as one additional sentence after the tool clauses — e.g. "Also use proactively — without the user naming a tool — to show the user visual content (HTML, diagrams, reports, a local dev server) in a browser window, or to push a notification to their devices (run-kit)." — mirroring the "Also use proactively when…" trigger idiom Claude Code's own built-in skills use. Keep SkillHint as the reactive task-domain phrase. CONSTRAINTS: the description MUST stay a single line (TestAgentSetup_DescriptionSingleLine); the Roster stays the single source of truth (extend TestRosterSkillHints or add a sibling test pinning run-kit's ProactiveHint); whatever content-hash/staleness marker the self-maintaining refresh uses (PR #50: update refresh + doctor staleness check) must change so `shll update`/doctor re-place the skill on existing machines. ALSO in the same edit: enrich run-kit's Roster.Description so hop-1 browsers see the capability (e.g. "Run-kit — tmux session manager with a web UI; can display web pages/HTML to the user and push notifications (rk stays as an alias)"), and consider one thin "proactive capabilities → `shll skill run-kit`" line in the bootstrap body (agentSkillContent) — body text loads only on activation so it is cheap, but keep the thin-bootstrap genre. VERIFY: after `shll agent-setup`, both ~/.agents/skills/shll-toolkit/SKILL.md and ~/.claude/skills/shll-toolkit/SKILL.md carry the new sentence, and a tool-name-free display-shaped prompt plausibly matches the description.
- [x] [tp2s] 2026-07-18: Support `shll skill <tool> <topic>` passthrough in the skill composer. PROBLEM: run-kit ships a topic page per the skill standard's topic-pages section (`rk skill display`, run-kit PR #386), but `shll skill run-kit display` fails with "accepts at most 1 arg(s), received 2" (usage error, exit 2) — the composer accepts exactly one arg. The placed bootstrap skill teaches `shll skill <tool>` as the route to depth; topic pages are then reachable only by switching to the tool's own binary (`rk skill display`) — the core bundle's topic index does name that command, so this is a uniformity gap in the shll front door, not a dead end, but if shll is the documented route the route should reach topics too. IMPLEMENTATION: accept an optional second arg and delegate to `<tool> skill <topic>` verbatim, preserving the standard's topic contract (raw markdown to stdout; stderr empty on success; unknown topic → non-zero exit with the valid topics on stderr — propagate, do not swallow or rewrap). Before extending, check the composer's documented grammar in docs/memory/cli/skill.md and the manager-exception/composer-grammar note flagged in fab/changes/260718-agst-agent-setup-skill-commands/intake.md (~line 124) so the standard text and the composer stay consistent; update the bootstrap body's step 2 and the `shll skill` trailer line ("Run 'shll skill <tool>' for…") to mention the topic form. TESTS: passthrough happy path (stub tool), unknown-topic stderr/exit propagation, arg-count contract (>2 args still exit 2).
- [x] [fw9d] 2026-07-18: Update the skill standard's stale "Forward design: shll agent-setup" paragraph — the design landed, via a different mechanism. docs/site/standards/skill.md:70 still reads "(Planned, not yet built — …)" and describes agent-setup as "aggregate every installed tool's core `<tool> skill` output into the agent's context". Change agst (shipped 2026-07-18, PRs #48/#50) landed `shll agent-setup` as thin-bootstrap SKILLS PLACEMENT (one shll-toolkit skill at ~/.agents/skills + ~/.claude/skills, description roster-driven) + the runtime two-step (`shll skill` glossary → `shll skill <tool>` bundle on demand), explicitly REJECTING context aggregation and per-tool bundle placement (bundles go stale between updates; listing lines multiply) — recorded in docs/memory/cli/standards-content.md §"Forward design — LANDED (change agst)" and cross-linked from docs/memory/cli/agent-setup.md:95. The agst intake itself flags this standard-text edit as a likely small follow-up (intake.md ~line 124). WORK: rewrite the paragraph as a landed-design note — the placement mechanism, the two-step, hook-wiring delegation to `run-kit agent-setup` (already hook-only today) — and REPRICE the budget rationale: the ≤150-line + static-only rules are now motivated by use-time `shll skill <tool>` pulls into a paying context (plus the `shll skill` glossary aggregating roster one-liners), not by N-bundle concatenation into one payload. The memory files are already correct — this is a docs/site/standards edit only; re-run the standards sync + drift-guard (the embedded copies must match byte-for-byte) and ship as a normal docs-type fab change. Cross-ref: pairs with [pv7t] (which relies on the placement design's description-as-trigger-surface being the documented mechanism).
- [ ] [tkch] 2026-06-13: Remove the HOMEBREW_NO_REQUIRE_TAP_TRUST=1 workaround once the upstream Homebrew Linux-sandbox bug is fixed. This is the cleanup follow-up to backlog item [38a6] (which baked the override into shll's brew install/upgrade/update calls). BACKGROUND: Homebrew 6.0's Linux bwrap sandbox masks ~/.homebrew (HOMEBREW_USER_CONFIG_HOME) via deny_read_home, so the sandboxed build.rb cannot read trust.json and wrongly raises UntrustedTapError when HOMEBREW_REQUIRE_TAP_TRUST=1 is set; the error is also swallowed (raised before build.rb connects its error pipe), surfacing as an opaque 'bwrap exited with 1'. shll injects HOMEBREW_NO_REQUIRE_TAP_TRUST=1 on its brew calls to keep installs working without disabling the sandbox. Two distinct upstream bugs feed this; EITHER being fixed may suffice, but verify before removing: (1) deny_read_home in Homebrew/brew Library/Homebrew/sandbox.rb should add an exception for HOMEBREW_USER_CONFIG_HOME (~/.homebrew) so the in-sandbox trust re-check can read trust.json; (2) build.rb should connect the error pipe BEFORE parsing args / running the trust check so early exceptions are not silently dropped. HOW TO VERIFY THE FIX LANDED (do this before deleting the workaround): on a Linux box with HOMEBREW_REQUIRE_TAP_TRUST=1 set and the tap trusted (~/.homebrew/trust.json lists sahil87/tap), run a clean 'brew upgrade sahil87/tap/idea' (or any roster tool) WITHOUT HOMEBREW_NO_REQUIRE_TAP_TRUST set, on the current Homebrew. If it completes without the bwrap/UntrustedTapError failure, the upstream fix is in. Track upstream at github.com/Homebrew/brew (search issues/PRs for sandbox deny_read_home HOMEBREW_USER_CONFIG_HOME / build.rb error pipe trust); if no issue exists, one should be filed (the diagnosis from change-of-2026-06-12 has the full repro). REMOVAL: revert the [38a6] changes — drop the HOMEBREW_NO_REQUIRE_TAP_TRUST=1 injection (the brewEnv()/Env-field wiring in proc.go + brew.go and its use at the install/update call sites), remove the now-stale code comments and the linux-gating, and delete the workaround unit tests (or flip them to assert NO override is set). If [38a6] added an 'Env' field to proc.Request that nothing else uses after removal, consider whether to keep it (harmless, generally useful) or strip it. Update spec/memory to drop the temporary-workaround note. Cross-ref: pairs with [38a6].
- [ ] [k4dn] 2026-07-19: Delete the stale SHLLAI_TOKEN repo secret from sahil87/shll settings — nothing references it since the help-push teardown (7huv); verified still present via `gh secret list` on 2026-07-19. (relocated from docs/memory/ci/release-workflow.md by /docs-distill-memory)
- [ ] [wq3f] 2026-07-19: Move the `runKitToolName = "run-kit"` constant from src/cmd/shll/install.go to agent_setup.go — its sole consumer is delegateRunKitAgentSetup after the agst nudge graduation — and fix its stale doc comment ("resolve its Tool descriptor from the live Roster" no longer describes any consumer). (relocated from docs/memory/cli/install.md by /docs-distill-memory)

## Toolkit standards rollout directives (std1 / std2 / std3)

Operator brief: Repos: `hop`, `wt`, `tu`, `idea`, `run-kit`, `fab-kit` (optionally `shll` itself — same amendment applies). Dispatch Directive 1 to all repos in parallel; there is no cross-repo dependency. Directive 2 dispatches per repo as soon as that repo's Directive-1 PR is merged — no need to wait for the full wave. Precondition for Directive 2 anywhere: a shll release carrying the `standards` subcommand (post docs/site/standards/ move) is published and installed on the worker machines (`shll standards` runs clean); if not, hold Directive 2.

### Directive 1 — Constitution amendment: toolkit standards conformance (paste per repo)

**Task: Amend this repo's fab constitution to bind it to the shll toolkit standards.**

This repo is part of the shll toolkit. The toolkit publishes binding, producer-facing standards — CLI design principles plus mechanical contracts (machine-readable help output, README/docs-site structure, and others over time). They are canonically authored in the `sahil87/shll` repository's `docs/site/` tree, rendered on https://shll.ai, and readable offline via the `shll standards` command. This change adds a constitution article so every future pipeline run in this repo loads and enforces the obligation.

Make this change:

1. In `fab/project/constitution.md`, add a new article under **Additional Constraints** (create the section if this constitution lacks it, matching the file's existing structure):

   > ### Toolkit Standards
   > This tool is part of the shll toolkit and MUST conform to the toolkit's published standards. The standards are enumerated by running `shll standards` — each entry names what it governs; read one with `shll standards <name>`. Before changing the CLI surface, help output, `README.md`, or `docs/site/`, the change MUST be checked against the standards governing that surface. If `shll` is unavailable, the canonical sources are the `sahil87/shll` repository's `docs/site/` tree (rendered on https://shll.ai). Standards added or revised there bind this repo without further amendment to this constitution.

2. Bump the constitution's `Last Amended` date (and version, per this file's own governance line).
3. Deliberate constraint: do NOT copy standard names, counts, or per-standard URLs into the constitution — `shll standards` is the enumeration, and the article must stay correct as standards evolve.

Ship per this repo's normal flow (docs-type fab change → PR). Nothing else is in scope — no conformance fixes in this change.

### Directive 2 — Conformance change (paste per repo, only after that repo's Directive-1 PR is merged)

**Task: Bring this repo and its tool into conformance with the shll toolkit standards.**

Precondition: `shll standards` runs on this machine (if the subcommand is missing, run `shll update`; if it still fails, stop and report — do not proceed from memory or the website). This repo's constitution carries the Toolkit Standards article; this task is the conformance work it mandates.

1. **Enumerate at runtime**: run `shll standards`, then `shll standards <name>` for every listed entry. The list is authoritative — do not assume which standards exist or what they require.
2. **Audit** this repo against each standard. For mechanical contracts (machine help output, README/docs-site structure), execute the standard's own verification checklist verbatim. For the principles, assess each numbered principle against the tool's actual behavior — prompts and TTY handling, stdout/stderr separation, `--json`/`--dry-run`/`--yes` coverage, exit codes and error wording, idempotency, output volume.
3. **Fix what is proportionate here**: all mechanical-contract violations, and principle gaps that are small and additive (a missing flag, a misrouted stream, an unhelpful error). Larger gaps that would restructure the tool are NOT for this change — record each as a draft change or issue per this repo's convention and reference it.
4. **Deliverable**: one fab change whose PR body contains a conformance report — one section per standard with PASS or the gaps found, each gap dispositioned as *fixed here* (with the commit) or *deferred to <ref>*. Include the shll version audited against (`shll version`'s shll row), since standards are versioned with the shll release. Tests green; if the command tree changed, re-verify the machine-help contract afterward.

### Directive 3 — Toolkit naming conformance: "shll toolkit" (paste per repo)

Operator brief: Repos: `hop`, `wt`, `tu`, `idea`, `run-kit`, `fab-kit` (`shll` itself is done — sahil87/shll#56). Dispatch to all repos in parallel; no cross-repo dependency or ordering, and no dependency on Directive 2's conformance state. Precondition everywhere: a shll release carrying sahil87/shll#56 is published and installed on the worker machines — verify with `shll standards readme-extraction`, which must show the blockquote `> Part of the [shll toolkit](https://shll.ai) — see all projects there.`. If it shows the old `@sahil87's open source toolkit` line, run `shll update` and re-check; if still old, hold this directive.

**Task: Conform this repo to the toolkit's standardized name — "shll toolkit".**

The toolkit formerly named "sahil87 toolkit" is now the **shll toolkit** (sahil87/shll#56). The readme-extraction standard's canonical README blockquote changed accordingly. This repo's constitution already binds it to revised standards without amendment — this task is the conformance work.

Precondition: `shll standards readme-extraction` runs on this machine and shows the new blockquote (below). If not, run `shll update`; if it still shows the old line, stop and report — do not proceed from memory.

Make this change:

1. **README blockquote** — replace the toolkit blockquote with this exact line, byte-identical, keeping the mandated head order (H1 → blockquote → badges): `> Part of the [shll toolkit](https://shll.ai) — see all projects there.`
2. **Prose sweep** — replace remaining `sahil87 toolkit` → `shll toolkit` and `sahil87 tool(s)` → `shll tool(s)` wherever they appear as prose: README, `docs/site/**` (including the skill bundle `docs/site/skill.md` if present), CLI help text and user-visible strings (update their test goldens), and `fab/project/` files. If this repo embeds docs in the binary (skill bundle or similar), re-run its sync step so drift-guard tests pass.
3. **Constitution (cosmetic, same PR)** — in the Toolkit Standards article, change "part of the sahil87 toolkit" to "part of the shll toolkit" and bump `Last Amended` per the file's governance line. Nothing else in the article changes.
4. **Do NOT touch identifiers**: `sahil87/tap` formula names, `github.com/sahil87/…` and `raw.githubusercontent.com/sahil87/…` URLs, the `sahil87/shll` canonical-source reference in the constitution article, and any GitHub-owner constants in code. Historical artifacts (`fab/changes/` archives) stay untouched.

Ship per this repo's normal flow (one fab change → PR). Tests green; if help text changed, the help-dump JSON shape is unchanged (text-only edits — no `schema_version` bump).
- [x] [hh4t] 2026-07-19: Add reusable bulk-tool orchestration commands to shll, stored in .claude/commands/ (NOT .claude/skills/ — that dir is entirely regenerated by fab-kit's embedded binary templates on every 'fab sync'/'wt create', so anything placed there is silently overwritten; .claude/commands/ is untouched by fab-kit sync and is the correct home). CONTEXT: the fab-operator role (see .claude/skills/fab-operator) frequently needs to run the same bulk operation across all 7 sahil87 toolkit repos (hop, wt, tu, idea, run-kit, fab-kit, shll) — e.g. 'run fab upgrade-repo in a fresh worktree in every repo, then ship a PR if anything changed' (done ad hoc in change 260718, see fab pane map history / operator session). Today each bulk op is improvised inline by the operator (manual per-repo loop: wt create --non-interactive, spawn fab agent --print --repo <repo> in a tmux window, dispatch /fab-new <description> via tmux load-buffer/paste-buffer, then /fab-fff, monitor via fab pane map --all-sessions --json, skip PR entirely if the op made no changes). WORK: (1) Author reusable slash-command(s) under shll's own .claude/commands/*.md (e.g. commands/bulk-upgrade-repo.md, or a more generic commands/bulk-op.md parameterized by a shell recipe) that codify this exact recipe as a repeatable operator primitive — the command should accept/document: the list of target repos (default: the 7-repo roster), the per-repo shell recipe to run in a fresh worktree, the change/no-change PR-skip rule, and how it plugs into fab-operator's monitored-set/tick-loop conventions (branch_map, monitored entries, window » / › prefix convention) so a spawned bulk op is trackable exactly like any other fab-operator change. (2) gitignore: shll's .gitignore currently has a blanket '/.claude' line (excludes the ENTIRE .claude directory including commands/) — this must be narrowed so .claude/commands/ (and only that subdir) is tracked/committed while .claude/skills/ (fab-kit-regenerated, must stay ignored to avoid churn/conflicts) and other .claude/* noise (settings.local.json, scheduled_tasks.lock) remain ignored. Likely shape: keep '/.claude/*' as the broad ignore then add '!/.claude/commands/' and '!/.claude/commands/**' as negation exceptions (verify Claude Code's own .gitignore precedent/convention for this pattern, and check whether settings.local.json under .claude/ needs an explicit re-ignore since '!' negations don't un-ignore descendants whose parent is excluded — may need '!/.claude/' then re-ignore specific subpaths instead, test both approaches for correctness). (3) Consider whether these commands are shll-specific or should also be deployed to the other 6 repos the same way fab-operator itself is (it's a .claude/skills file, deployed via fab sync's project-sync mechanism) — if bulk ops are operator-only (invoked from the operator's own pane, not from within each repo), they may only need to exist in shll and never need multi-repo deployment; if they need to be invoked from inside any individual repo's session, deployment parity with fab-operator matters. VERIFY: after adding the gitignore exception, confirm 'git status' shows .claude/commands/*.md as trackable (not ignored) while .claude/skills/** and .claude/settings.local.json remain ignored.
