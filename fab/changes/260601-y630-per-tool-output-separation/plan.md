# Plan: Per-Tool Output Separation for shll update/install/shell-init

**Change**: 260601-y630-per-tool-output-separation
**Status**: In Progress
**Intake**: `intake.md`
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-continue at the apply stage entry. Walks spec.md once,
  emitting a Task + an Acceptance entry per requirement. Apply consumes only
  `## Tasks`; review consumes only `## Acceptance`.
-->

## Tasks

<!-- Sequential work items for the apply stage. Checked off [x] as completed. -->

### Phase 1: Setup

<!-- Dependency + new shared helper file. -->

- [x] T001 Add `golang.org/x/term` as a direct dependency: run `go get golang.org/x/term` then `go mod tidy` from `src/` (where `go.mod` lives), so `src/go.mod` and `src/go.sum` record it. <!-- A-006 -->
- [x] T002 Create `src/cmd/shll/ui.go` with: named SGR constants (`ansiBoldCyan`, `ansiGreen`, `ansiBold`, `ansiReset`); `colorEnabled(w io.Writer) bool` (TTY via `term.IsTerminal` on `*os.File` AND `NO_COLOR` unset); `printToolHeader(w io.Writer, name string, color bool)` (`▸ <name>` colored / `==> <name>` plain); `toolComment(name string) string` returning `# ── <name> ──` (always plain ASCII); and a summary-tail helper rendering `Done — N of M tools succeeded.` / `X succeeded, Y failed — see above.` with green `✓` when color. No external color lib, no magic ANSI strings. <!-- A-001 A-002 A-003 A-004 A-005 A-007 -->

### Phase 2: Core Implementation

<!-- Wire the helpers into the three commands. -->

- [x] T003 In `src/cmd/shll/update.go`: compute the color decision once via `colorEnabled(stdout)`; print `printToolHeader(stdout, "shll (self)", color)` before the self-upgrade `brew upgrade shllFormula` step (only when `shllSelfInstalled`); print `printToolHeader(stdout, t.Name, color)` before each roster tool's `upgradeTool` call in the loop; track per-tool success/failure counts; after the loop print the summary tail to `stdout`. Keep `updateStatusLine` first and unchanged; do NOT change `anyFailed`/`errSilent`/exit-code logic. The empty-case short-circuit (`No sahil87 tools installed.`) runs no loop → no header, no tail. <!-- A-008 A-009 A-011 A-013 A-016 -->
- [x] T004 In `src/cmd/shll/install.go`: mirror update — compute color once via `colorEnabled(stdout)`; print `printToolHeader(stdout, t.Name, color)` before each missing tool's `brew install`; track success/failure counts; print the summary tail to `stdout` after the loop. Empty-case (`All sahil87 tools already installed.`) → no header, no tail. Preserve `anyFailed`/`errSilent`/exit codes. <!-- A-010 A-014 A-017 -->
- [x] T005 In `src/cmd/shll/shell_init.go`: before writing each tool's init block to stdout — only on the success-write path (installed AND non-erroring) — emit `toolComment(tool.Name)` followed by a newline to stdout, then the tool's output. Do NOT emit for `ErrNotFound`-skipped tools or errored tools (stdout dropped). No color, no TTY-gating, no tail. Preserve eval-safety and `errSilent`/exit codes. <!-- A-018 A-019 A-020 -->

### Phase 3: Tests (Golden-String Conformance)

<!-- Conform tests to the spec-defined output; add focused new tests for new scenarios. -->

- [x] T006 Update `src/cmd/shll/update_test.go`: loop-path tests now expect per-tool `==> <tool>` headers and the plain summary tail on stdout. `TestUpdate_NoToolsInstalled` golden string (`Checking…\nNo sahil87 tools installed.\n`) stays verbatim (empty case, no header/tail). `TestUpdate_StatusLinePrecedesProbes` still asserts the status line leads. Add a focused test that the self-upgrade `==> shll (self)` header and a per-tool header are present and that header text appears in stdout (not stderr). <!-- A-012 A-015 A-021 A-023 -->
- [x] T007 Update `src/cmd/shll/install_test.go`: `TestInstall_AllAlreadyInstalled` golden string stays verbatim (empty case). Add/adjust assertions that loop-path runs emit `==> <tool>` headers and the plain tail to stdout, with no header/tail text on stderr. <!-- A-015 A-022 -->
- [x] T008 Update `src/cmd/shll/shell_init_test.go`: the concatenation golden strings (e.g. `TestShellInit_ZshAllIntegratorsInstalled`, `TestShellInit_DeterministicOrder`, `TestShellInit_OnlyTuInstalled`, `TestShellInit_SubToolFailure`) now interleave `# ── <tool> ──\n` separators before each contributing tool's block. Uninstalled and erroring tools get no separator. `TestShellInit_NoIntegratingToolsInstalled` stays empty. Confirm `version_test.go` and `shell_install_test.go` remain untouched. <!-- A-018 A-019 A-020 A-022 A-024 -->

### Phase 4: Build & Verify

- [x] T009 From `src/`: `gofmt`/`go vet` clean, `go build ./...` succeeds, `go test ./...` passes (scope to `./cmd/shll/...` first, then full). <!-- A-025 -->

## Execution Order

- T001 → T002 (helper uses `golang.org/x/term`).
- T002 blocks T003, T004, T005 (commands consume the helpers).
- T003/T004/T005 are independent of each other (different files) but each precedes its own test update.
- T006 depends on T003; T007 depends on T004; T008 depends on T005.
- T009 runs last (whole-tree build/vet/test).

## Acceptance

### Functional Completeness

- [ ] A-001 Single shared UI helper file: `src/cmd/shll/ui.go` exists and holds TTY detection, `NO_COLOR` check, glyph/style choice, per-tool header printer, summary-tail printer, and the shell-init comment emitter; `update.go`/`install.go`/`shell_init.go` consume it with no duplicated TTY/`NO_COLOR`/glyph logic.
- [ ] A-002 Color and Unicode gating: glyphs/color emitted only when stdout is a TTY (`term.IsTerminal` via `golang.org/x/term`) AND `NO_COLOR` is unset; otherwise plain ASCII with no ANSI.
- [ ] A-003 Injectable color seam: the color decision is a parameter (or equivalent seam) so `bytes.Buffer` test writers naturally hit the plain branch and assert `==>` / `# ── … ──` / plain-tail forms without faking a TTY.
- [ ] A-004 Hand-rolled ANSI SGR named constants: styling uses named SGR constants in `ui.go` (no external color library, no inline magic escape strings).
- [ ] A-005 ASCII degrade swaps shll's own Unicode: plain branch swaps `▸`→`==>` and any shll-own Unicode (e.g. `→`→`->`); sub-tool bytes are passed through untouched in both forms.
- [ ] A-006 New direct dependency: `golang.org/x/term` is the only added direct dependency in `src/go.mod` (+ `go.sum`).

### Behavioral Correctness

- [ ] A-007 `toolComment` emits `# ── <name> ──` always-plain ASCII with no color and no TTY-gating, regardless of `NO_COLOR` or terminal state.
- [ ] A-008 `shll update` prints `▸ <tool>` (TTY+color) / `==> <tool>` (plain) immediately before each roster tool's foregrounded output, in roster order.
- [ ] A-009 `shll update` prints a `shll (self)` header (`▸`/`==>`) before the self-upgrade step when `isInstalled(ctx, shllFormula)` is true.
- [ ] A-010 `shll install` prints a per-tool header before each missing tool's install output, in roster order.
- [ ] A-011 `updateStatusLine` (`Checking installed sahil87 tools…`) remains unchanged and is still the first byte to stdout, before any per-tool header.
- [ ] A-012 Headers and tail are written to stdout (the same stream as foregrounded output), never to stderr.
- [ ] A-013 `shll update`/`shll install` print exactly one summary tail derived from exit codes: `Done — N of M tools succeeded.` on full success, `X succeeded, Y failed — see above.` on partial failure; counts align with existing `anyFailed` semantics; the tail never changes the exit code and never claims "updated"/"up-to-date".
- [ ] A-014 The summary tail is color-gated like the headers (green `✓` on a TTY, plain otherwise); `shell-init` and `version` print no tail.

### Removal Verification

- [ ] A-015 `version.go` and `version_test.go` are untouched; `shell_install_test.go` is untouched.

### Scenario Coverage

- [ ] A-016 `update` empty-case (`No sahil87 tools installed.`): stdout is exactly `Checking installed sahil87 tools…\nNo sahil87 tools installed.\n` with no header and no tail; golden string unchanged.
- [ ] A-017 `install` empty-case (`All sahil87 tools already installed.`): stdout is exactly that line, no header, no tail; golden string unchanged.
- [ ] A-018 `shell-init` interleaves `# ── tu ──`, tu's block, `# ── hop ──`, hop's block, `# ── wt ──`, wt's block in roster order when all three are installed and emit output.
- [ ] A-019 `shell-init` separator is plain ASCII even on a color-enabled TTY (no ANSI), keeping output eval-safe.
- [ ] A-020 `shell-init` emits a separator only for tools whose output reaches stdout (installed + non-erroring); uninstalled (`ErrNotFound`) and erroring tools get no separator and the command's `errSilent`/exit behavior is unchanged.
- [ ] A-021 A test asserts the per-tool header precedes the tool's output ordering (header-before-output).
- [ ] A-022 Loop-path golden strings in `update_test.go`/`install_test.go`/`shell_init_test.go` are updated to include the `==>` / `# ── … ──` / tail markers and pass against the spec-defined output.

### Edge Cases & Error Handling

- [ ] A-023 Header text appears only in the stdout buffer, not the stderr buffer, when a test drives `runUpdate`/`runInstall` with separate buffers.
- [ ] A-024 `shell-init` with no integrating tools installed produces empty stdout (no separators), exit 0 — unchanged.

### Code Quality

- [ ] A-025 `gofmt`/`go vet` clean; `go build ./...` and `go test ./...` pass from `src/`.
- [ ] A-026 Pattern consistency: new code follows the codebase's naming, error-handling, and test-seam idioms (e.g. the swappable `proc.Runner` precedent, named constants per code-quality.md).
- [ ] A-027 No unnecessary duplication: the TTY/`NO_COLOR`/glyph logic lives only in `ui.go`; subprocess invocation still routes only through `internal/proc` (no new `os/exec`); no sub-tool logic reimplemented (Constitution III/IV).

## Notes

- Check items as you review: `- [x]`
- All acceptance items must pass before `/fab-continue` (hydrate)
- If an item is not applicable, mark checked and prefix with **N/A**: `- [x] A-NNN **N/A**: {reason}`
