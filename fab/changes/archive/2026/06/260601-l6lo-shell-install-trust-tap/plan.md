# Plan: shell-install --trust-tap (Homebrew tap-trust resolution)

**Change**: 260601-l6lo-shell-install-trust-tap
**Status**: In Progress
**Intake**: `intake.md`
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-continue at apply entry. Apply parses ## Tasks; review parses ## Acceptance.
-->

## Tasks

### Phase 1: Setup (constants + ceremony seam in brew.go)

- [x] T001 Add a `tapName` constant (`"sahil87/tap"`, no trailing slash) to `src/cmd/shll/brew.go` (or `tools.go`), with a doc comment noting it is the tap argument for `brew trust --tap`, distinct from `formulaPrefix` = `sahil87/tap/`. <!-- A-008 -->
- [x] T002 In `src/cmd/shll/brew.go`, add `brewTrustAvailable(ctx) bool` — capability-probe via `proc.Run(ctx, brewBinary, "trust", "--help")`, returning false on `proc.ErrNotFound` (brew absent) and false when the subcommand is unrecognized (probe errors or output indicates no such command). Mirror the `hasBrew` / `toolSupportsSkipFlag` precedent. <!-- A-005, A-010 -->
- [x] T003 In `src/cmd/shll/brew.go`, add `brewTrustTap(ctx) (int, error)` — invoke `brew trust --tap sahil87/tap` (using `tapName`) via `proc.RunForeground` so the user sees brew output; return its exit code/error. Invoked unconditionally by callers (idempotency verified). <!-- A-003, A-004 -->

### Phase 2: Core Implementation (combined sentinel + per-line block machinery in shell_install.go)

- [x] T004 In `src/cmd/shll/shell_install.go`, introduce the new sentinel constants `newOpenSentinel = "# >>> shll >>>"` / `newCloseSentinel = "# <<< shll <<<"` and the export-line constant `exportTrustLine = "export HOMEBREW_REQUIRE_TAP_TRUST=1"`. Keep the legacy `openSentinel`/`closeSentinel` constants for migration recognition. Add a `legacyOpenSentinel`/`legacyCloseSentinel` naming if clearer; the eval body format constant stays. <!-- A-001, A-008 -->
- [x] T005 In `src/cmd/shll/shell_install.go`, generalize block building: a `buildBlockBody(lines []string) []byte` that wraps an ordered, deduplicated set of managed lines (export first, then eval) in the new sentinel pair with a single trailing `\n`. Keep `buildBlock(shell)` behavior available for the eval-only default-shell case if still referenced, but route it through the new builder using the new sentinels. <!-- A-001, A-009 -->
- [x] T006 In `src/cmd/shll/shell_install.go`, add block-location/parsing helpers that recognize BOTH the new `# >>> shll >>>` sentinel and the legacy `# >>> shll shell-init >>>` sentinel: locate the block range, extract managed lines (eval line, export line) from an existing block body, and detect an open-without-close (partial) block for either sentinel. <!-- A-006, A-013 -->

### Phase 3: Integration & Edge Cases (flag wiring, merge, ceremony, degradation, uninstall)

- [x] T007 In `src/cmd/shll/shell_install.go`, register the `--trust-tap` bool flag in `newShellInstallCmd` and thread `trustTap` through `runShellInstall` to the default and print paths. Update the `Long` help text to document `--trust-tap` (composes with default/`--print`/`--uninstall`). Keep `--print`/`--uninstall` mutual-exclusion guard unchanged (exit 2). <!-- A-001, A-002 -->
- [x] T008 In `src/cmd/shll/shell_install.go`, rewrite `runShellInstallDefault` as a per-line merge: stat (no O_CREATE), read content, detect partial/unclosed legacy-or-new block → refuse with diagnostic + exit 2; otherwise compute desired managed lines (eval always; export when `--trust-tap` AND trust applied — see T010), union with any existing block's lines, rewrite the block in place under the new sentinel (migrating legacy sentinel), or append a fresh block when none exists. Preserve trailing-newline guard, O_APPEND-for-append symlink preservation, and EvalSymlinks→O_TRUNC for in-place rewrite. Byte-identical no-op when nothing changes (per-line idempotency message). <!-- A-001, A-009, A-011, A-012, A-013, A-014 -->
- [x] T009 In `src/cmd/shll/shell_install.go`, handle both-sentinels-present (legacy + new) by merging the union of managed lines from both into ONE new-sentinel block and removing the legacy block (self-healing, exit 0). <!-- A-013 -->
- [x] T010 In `src/cmd/shll/shell_install.go`, wire the ceremony + degradation into the `--trust-tap` default path WITHOUT importing `internal/proc`/`os/exec` (call the `brew.go` helpers via the seam): when `--trust-tap`, probe `brewTrustAvailable`; if available, run `brewTrustTap` — on success include the export line in the desired set; on ceremony failure (non-zero/err) OR when unavailable/brew-absent, do NOT include the export line, emit a diagnostic naming `HOMEBREW_NO_REQUIRE_TAP_TRUST=1` / `HOMEBREW_NO_ENV_HINTS=1` and that trust was skipped, but STILL write the eval line; exit 0 (degraded success). The default `runShellInstall` seam must accept a small ceremony/probe function value or call exported `brew.go` functions so `shell_install.go` itself never imports proc. <!-- A-002, A-003, A-004, A-010, A-016 -->
- [x] T011 In `src/cmd/shll/shell_install.go`, update `runShellInstallPrint` for `--trust-tap`: print the resulting combined block (export + eval) to stdout under the new sentinel; modify no file and run no ceremony. <!-- A-002 -->
- [x] T012 In `src/cmd/shll/shell_install.go`, update `runShellInstallUninstall` to remove the whole block recognizing BOTH the new and legacy sentinels; do NOT run `brew untrust`; preserve EvalSymlinks→O_TRUNC symlink preservation and the missing-file/benign-no-op semantics. <!-- A-001, A-009, A-015 -->

### Phase 4: Tests + Docs

- [x] T013 [P] Add ceremony tests in a new `src/cmd/shll/brew_test.go` using the `fakeRunner` / `installFakeRunner` pattern (from `update_test.go`): `brewTrustAvailable` true/false/`ErrNotFound`; `brewTrustTap` builds `brew trust --tap sahil87/tap` (assert `tapName`, not a formula) and surfaces exit code/error. <!-- A-003, A-004, A-005, A-010 -->
- [x] T014 Extend `src/cmd/shll/shell_install_test.go`: update existing block-format/idempotency/install/uninstall tests to the new `# >>> shll >>>` sentinel; add cases — `--trust-tap` fresh combined block (export before eval), shell-init-only new sentinel, merge export into eval-only block, merge eval into export-only block, full re-run byte-identical no-op, legacy eval-only migration on `--trust-tap`, both-sentinels-present merge, partial/unclosed block refuse (exit 2), uninstall new + legacy block, `--print --trust-tap` combined output (no file/ceremony), degradation (trust unavailable / brew absent / ceremony non-zero → eval written, no export, exit 0). Use a proc.Runner fake for the trust-path cases that exercise the ceremony seam. Keep `TestNoProcImports` passing. <!-- A-001..A-016 -->
- [x] T015 [P] Update `README.md`: document `--trust-tap` in the `shll shell-install` section (and update the shown sentinel block to `# >>> shll >>>` / `# <<< shll <<<`), and add a Troubleshooting section explaining the tap-trust warning (brew env-hint, surfaced because shll wraps brew, may show 2–3×), with `shll shell-install --trust-tap` as the resolution and `HOMEBREW_NO_REQUIRE_TAP_TRUST=1` / `HOMEBREW_NO_ENV_HINTS=1` as user-set alternatives — stating shll won't set them for the user. <!-- A-007 -->

## Execution Order

- T001–T003 (brew.go seam) before T010 (which calls them).
- T004–T006 (block machinery) before T007–T012.
- T008 depends on T004/T005/T006; T009 depends on T006; T010 depends on T002/T003 + T008.
- T013–T015 after their implementation targets; T013 and T015 are independent ([P]).

## Acceptance

### Functional Completeness

- [ ] A-001 New `--trust-tap` flag composes with modes: `shll shell-install --trust-tap` is accepted (no unknown-flag error), composes with default/`--print`/`--uninstall`, and `--help` documents it.
- [ ] A-002 Genuine trust applies both halves: default `--trust-tap` runs `brew trust --tap sahil87/tap` AND ensures `export HOMEBREW_REQUIRE_TAP_TRUST=1` in the block; `--print --trust-tap` prints the combined block, writes nothing, runs no ceremony.
- [ ] A-003 Ceremony routes through `internal/proc` (`proc.Run`/`proc.RunForeground`) from `brew.go`, never `exec.Command`/`exec.CommandContext` directly.
- [ ] A-004 Ceremony is invoked unconditionally during `--trust-tap` (no trust pre-check); re-trusting an already-trusted tap is treated as success.

### Behavioral Correctness

- [ ] A-005 `brewTrustAvailable` capability-probe returns false on brew-absent (`proc.ErrNotFound`) and on an unrecognized `trust` subcommand; true when available.
- [ ] A-006 Install is a per-line MERGE: the block body is the union of existing managed lines and lines this invocation adds; no managed line is duplicated; a fully-present block + trusted tap is a byte-identical no-op (exit 0).
- [ ] A-016 Degradation: when trust is unavailable / brew absent / ceremony returns non-zero, the export line is NOT written, a diagnostic names the env-var escape hatches, the eval line IS still written, and the command exits 0.

### Removal Verification

- [ ] A-013 Legacy sentinel handling: a legacy `# >>> shll shell-init >>>` block is migrated in place to the new `# >>> shll >>>` / `# <<< shll <<<` sentinel (eval carried forward, export merged when `--trust-tap`); no legacy sentinel remains; both-sentinels-present merges into one new block.

### Scenario Coverage

- [ ] A-009 Combined block format: new sentinel pair `# >>> shll >>>` / `# <<< shll <<<` (note close uses `<<<`), export line before eval line, single trailing `\n`; shell-init-only install uses the new sentinel with only the eval line.
- [ ] A-010 `brew trust` / capability-probe live in `brew.go` (which imports `internal/proc`), exposed as functions `shell_install.go` calls; `shell_install.go` adds neither `internal/proc` nor `"os/exec"`.
- [ ] A-015 `--uninstall` removes the whole block recognizing BOTH new and legacy sentinels, does NOT run `brew untrust`, preserves the rc-file symlink, and is a benign no-op when no block / file is present (exit 0).

### Edge Cases & Error Handling

- [ ] A-011 Exit-code policy preserved: 0 success/no-op, 1 I/O failure (`errSilent`), 2 user-invocation error (`errExitCode{code:2}`); `--print`/`--uninstall` mutual-exclusion message unchanged.
- [ ] A-012 Symlink + trailing-newline + never-creates-rc-files invariants preserved (O_APPEND on append; EvalSymlinks→O_TRUNC on in-place rewrite/uninstall; no O_CREATE; trailing-`\n` guard).
- [ ] A-014 Partial/unclosed legacy-or-new block → clear diagnostic, refuse to modify, exit 2; no malformed block produced.

### Code Quality

- [ ] A-007 README documents the `--trust-tap` flag and a Troubleshooting section (warning is a brew env-hint, `--trust-tap` resolution, env-var alternatives the user may set, shll won't set them); shown sentinel updated to the new one.
- [ ] A-008 No magic strings: `tapName` is a named constant distinct from `formulaPrefix`; the new sentinels/export line are named constants.
- [ ] A-017 Pattern consistency: new code follows the existing `runShellInstall*` seam, cobra flag registration, `proc.Run`/`RunForeground` usage, and test-alongside conventions; reuses existing helpers (`hasBrew` precedent, `findBlock` range logic) rather than reimplementing.

### Security

- [ ] A-018 Constitution I preserved: every new subprocess invocation (capability probe + trust ceremony) routes through `internal/proc`; `TestNoProcImports` still passes for `shell_install.go`.

## Notes

- `## Acceptance` is consumed by review only — apply ignores it.
