# Plan: Reorder Roster to Leaves-First Dependency Order

**Change**: 260601-auvj-reorder-roster-leaves-first
**Status**: In Progress
**Intake**: `intake.md`
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-continue (or /fab-ff/fab-fff) at the apply stage entry.

  This file replaces the legacy `tasks.md` + `checklist.md` pair. Both apply and
  review consume sections of this single file:

    - Apply parses the `## Tasks` section (and only that section).
    - Review parses the `## Acceptance` section (and only that section).

  Section headings (`## Tasks`, `## Acceptance`) are the stable parser contract.
  Phase/category subheadings under each are presentational and may vary per change.

  TASK FORMAT: - [ ] T{NNN} [{markers}] {Description with file paths}
  ACCEPTANCE FORMAT: - [ ] A-{NNN} {declarative outcome}
-->

## Requirements

<!-- migrated from spec.md on 2026-06-02 -->

## Non-Goals

- **NOT a correctness fix.** Brew already resolves formula dependencies correctly and idempotently inside each tool's own `brew upgrade`; each `<tool> update` is self-update-only and no tool's `update` cascades into another tool's upgrade. The roster order cannot break or improve that. This change is output-coherence polish, not a behavior repair.
- **NOT a `DependsOn` data model.** The `Tool` struct gains no `DependsOn` field. Modeling the inter-tool dependency graph as data inside shll is rejected (Constitution III/VII) — the roster *list* is the contract; the dependency relationships stay implicit in slice order and are enforced only by a test.
- **NOT a runtime brew-deps query.** shll SHALL NOT call `brew deps` (or any runtime discovery) to derive the order — the order is statically encoded in the literal, consistent with the hardcoded-roster constitution.
- **NOT a new subcommand or flag.** No top-level surface area is added (Constitution VII), so no new-command justification is required.
- **NOT an update-only iteration order.** No second ordering is introduced for `shll update` alone — the single shared `Roster` is reordered so all three consumers (`update`, `install`, `shell-init`) share one source of truth.

## Roster: Ordering Contract

### Requirement: Leaves-First Roster Order

The `Roster` slice in `src/cmd/shll/tools.go` SHALL be declared in leaves-first dependency order: `wt`, `idea`, `tu`, `rk`, `hop`, `fab-kit`. Every tool that depends on another (by brew-upgrade or by runtime invocation) MUST appear after all of its dependencies in the slice. Only the entry order changes; each entry's `Name`, `Formula`, `ShellInit`, and `Update` fields MUST remain unchanged.

#### Scenario: Declared order is leaves-first

- **GIVEN** the `Roster` literal in `src/cmd/shll/tools.go`
- **WHEN** its entries are read top to bottom
- **THEN** the order is exactly `wt`, `idea`, `tu`, `rk`, `hop`, `fab-kit`
- **AND** `wt`, `idea`, and `tu` (the leaves — no outgoing dependency edges) precede `rk`, `hop`, and `fab-kit` (the dependents)

#### Scenario: Per-entry fields are preserved

- **GIVEN** the reordered `Roster`
- **WHEN** any entry is compared field-by-field against its pre-change definition
- **THEN** its `Name`, `Formula`, `ShellInit`, and `Update` values are identical
- **AND** no field is added to the `Tool` struct (no `DependsOn`)

### Requirement: Rationale Documented in the Roster Comment

The `Roster` doc comment SHALL explain the leaves-first dependency rationale. The current comment mentions only shell-init sequencing; it MUST be extended so a future reader understands that order also exists to keep each tool's per-tool output section (`shll update` / `shll install`) complete before any dependent's `brew upgrade` can re-touch it.

#### Scenario: Comment explains leaves-first intent

- **GIVEN** the `Roster` doc comment after this change
- **WHEN** a maintainer reads it
- **THEN** it states that order is leaves-first so dependents are processed after their dependencies
- **AND** it does not claim the order changes upgrade *correctness* (which brew owns)

## Roster: Invariant Test

### Requirement: TestRosterLeavesBeforeDependents Encodes All Dependency Edges

A test `TestRosterLeavesBeforeDependents` in `src/cmd/shll/tools_test.go` SHALL encode the full dependency graph as data and assert, for every edge `dependent -> dep`, that the dependent's index in `Roster` is strictly greater than the dep's index. The encoded edges MUST cover both kinds and be labeled by kind via comments:

- `fab-kit -> wt` — `// brew-upgrade dep`
- `fab-kit -> idea` — `// brew-upgrade dep`
- `hop -> wt` — `// brew-upgrade dep` AND `// runtime-invocation dep` (`hop open` delegates to wt's menu; `hop ls --trees` fans out `wt list --json`)
- `rk -> wt` — `// runtime-invocation dep` (`rk riff` shells out to `wt create`)

The test SHALL build a `name -> roster index` map from `Roster`, then check each edge. On violation, the failure message MUST name the offending edge with both indices (e.g. `"fab-kit (index N) must come after wt (index M)"`) so a future re-alphabetize or accidental reorder fails loudly and legibly.

#### Scenario: All edges satisfied by the leaves-first order

- **GIVEN** the `Roster` in leaves-first order
- **WHEN** `TestRosterLeavesBeforeDependents` runs
- **THEN** every encoded edge satisfies `index[dependent] > index[dep]`
- **AND** the test passes

#### Scenario: A regression to declaration/alphabetical order fails loudly

- **GIVEN** a hypothetical reorder that places a dependent before one of its deps (e.g. `fab-kit` ahead of `wt`)
- **WHEN** `TestRosterLeavesBeforeDependents` runs
- **THEN** the test fails
- **AND** the failure message names the specific offending edge and both indices

### Requirement: Test States the Contract Is a Superset of Output-Coherence

The invariant test SHALL carry a comment clarifying that it guards the toolkit's full ordering contract (brew-upgrade AND runtime-invocation edges) — a superset of what output-coherence strictly requires (which depends only on the brew-upgrade edges). The comment MUST prevent a reader from inferring that a runtime edge (e.g. `rk -> wt`) means `rk update` touches `wt` during `shll update` (it does not).

#### Scenario: Reader is not misled about runtime edges

- **GIVEN** the encoded `rk -> wt` runtime edge
- **WHEN** a maintainer reads the test
- **THEN** a comment makes clear this edge reflects runtime invocation (`rk riff -> wt create`), not an `rk update`-time upgrade of `wt`

## CLI: Update — Order-Sensitive Tests

### Requirement: update_test.go Conforms to the New Roster Order

`shll update` iterates `Roster` in order for its probe results, per-tool `▸`/`==>` headers, and partial-install sequencing. The order-sensitive assertions in `src/cmd/shll/update_test.go` SHALL be updated so the expected sequence matches the new leaves-first order. These are test-fixture updates to match the re-declared order (Test Integrity: the spec/declared order is the source of truth; tests conform — never the reverse). No `update.go` implementation logic changes.

#### Scenario: Header/sequence assertions reflect leaves-first order

- **GIVEN** a fully-installed roster
- **WHEN** `shll update` runs and `update_test.go` asserts the order of per-tool headers / recorded upgrade calls
- **THEN** the expected order is `wt`, `idea`, `tu`, `rk`, `hop`, `fab-kit` (after the `shll (self)` step)
- **AND** the assertions pass without any change to `update.go`

#### Scenario: Order-independent invariants are untouched

- **GIVEN** the update behavior contract (brew-missing bail, status line, single `brew update`, self-upgrade-before-roster, best-effort loop, summary tail, exit codes)
- **WHEN** the roster is reordered
- **THEN** those invariants are unchanged
- **AND** golden strings that do not depend on inter-tool order (e.g. the `No sahil87 tools installed.` empty case) remain verbatim

## CLI: Install — Order-Sensitive Tests

### Requirement: install_test.go Conforms to the New Roster Order

`shll install` partitions and installs missing tools in `Roster` order, mirroring update's header/sequence framing. The order-sensitive assertions in `src/cmd/shll/install_test.go` SHALL be updated to the new leaves-first order, as test-fixture conformance — no `install.go` logic changes.

#### Scenario: Install sequence reflects leaves-first order

- **GIVEN** none of the roster tools are installed
- **WHEN** `shll install` runs and `install_test.go` asserts the order of per-tool headers / recorded `brew install` calls
- **THEN** the expected order is `wt`, `idea`, `tu`, `rk`, `hop`, `fab-kit`
- **AND** the assertions pass without any change to `install.go`

## CLI: Shell-Init — Composition Order

### Requirement: Integrator Concatenation Order Becomes wt, tu, hop

`shll shell-init` concatenates the output of installed integrators in `Roster` order (`runShellInit` iterates `Roster` in ascending index order). With the new order, the three shell-integrating tools appear in roster positions `wt` (index 0), `tu` (index 2), `hop` (index 4) — so the emitted concatenation order flips from `tu, hop, wt` to `wt, tu, hop` (ascending by index). The `shell_init_test.go` golden assertions SHALL be updated to this new order. Each block's `# ── <tool> ──` separator and the eval-safety invariants are unchanged; only the inter-block order changes.

#### Scenario: All integrators installed concatenate in wt, tu, hop order

- **GIVEN** `tu`, `wt`, and `hop` all installed
- **WHEN** `shll shell-init zsh` runs
- **THEN** stdout is `# ── wt ──` + wt's block, then `# ── tu ──` + tu's block, then `# ── hop ──` + hop's block
- **AND** the determinism guarantee (byte-identical output across consecutive runs) still holds

#### Scenario: Eval-safety is preserved across the reorder

- **GIVEN** the reordered composition
- **WHEN** any integrator is missing or errors
- **THEN** stdout remains eval-safe (no `▸`/`==>` header, no color, only `#`-prefixed separators and successful sub-tool bytes)
- **AND** a tool that is absent or errors emits neither its block nor its separator

## CLI: Version — Order-Sensitivity Verification

### Requirement: shll version Needs No Golden-String Change

`shll version` iterates `Roster` and prints one self-labeled line per tool. Its tests assert each printed line against the *same* `Roster` they iterate (index-paired: `lines[i+1]` is checked against `Roster[i].Name`), so reordering `Roster` reorders expected and actual in lockstep. No `version_test.go` change is therefore required. This was the intake's Open Question; it is resolved here as a verification item, not an ambiguity — confirmed by inspection of `version_test.go` (`for i, tool := range Roster { ... lines[i+1] ... }`).

#### Scenario: Version output stays correct after the reorder

- **GIVEN** the reordered `Roster`
- **WHEN** `shll version` runs with all tools installed
- **THEN** each line self-labels with its tool name and version
- **AND** `version_test.go` passes unchanged because its assertions are paired to `Roster` order rather than to a hardcoded sequence

## Design Decisions

1. **Reorder the shared `Roster` (not an update-only iteration order)**: Statically reorder the single shared `Roster` literal to leaves-first; all three consumers (`update`, `install`, `shell-init`) inherit the order.
   - *Why*: One source of truth. The user explicitly chose this over a second, update-scoped ordering during the discussion. A shared order keeps `version`, `install`, and `shell-init` consistent with `update` and avoids two diverging lists to maintain.
   - *Rejected*: An `update`-only iteration order (keeping `Roster` as declared and sorting at iteration time in `update.go`). Adds a second ordering concept, more code, and a divergence risk for marginal isolation benefit.

2. **Guard with an invariant test, not a comment-only note**: Add `TestRosterLeavesBeforeDependents` encoding all edges (brew + runtime) labeled by kind, asserting `index[dependent] > index[dep]` with an edge-naming failure message.
   - *Why*: A comment cannot fail CI. A future re-alphabetize or careless reorder must fail loudly and point at the exact offending edge. Encoding runtime edges too (a superset of what output-coherence needs) documents the toolkit's full ordering contract in executable form.
   - *Rejected*: A comment-only guard above the literal. No enforcement — silently rots the moment someone reorders for unrelated reasons.

3. **No `DependsOn` field on the `Tool` struct (Constitution III/VII)**: Keep the dependency model implicit in slice order plus the invariant test; do not add data fields or a runtime topological sort.
   - *Why*: shll is a meta-tool whose contract is the hardcoded roster *list* (Constitution III). It should not own a data model of how the tools relate to each other; brew already owns the brew graph, and runtime edges are the sub-tools' concern. Constitution VII argues against expanding shll's surface for something a static order + test already satisfies.
   - *Rejected*: (a) Add `DependsOn []string` and topologically sort at runtime — models the inter-tool graph as shll-owned data, more code, more failure modes. (b) Query `brew deps` at runtime — more brew coupling, more latency, runtime discovery the constitution discourages for roster concerns.

4. **Frame as output coherence, not correctness**: The spec, scenarios, and Non-Goals explicitly state this is presentation polish.
   - *Why*: The premise that "fab-kit update gets wt also updated" was pressure-tested and refuted — each `<tool> update` is self-update-only, and brew resolves formula deps idempotently. The only observable effect of the old order was that a dependent's internal `brew upgrade` could re-touch a leaf already reported done under its own `▸` header, under-representing the per-tool framing (change y630). Over-claiming a correctness benefit would mislead the review stage.
   - *Rejected*: Framing this as a bug fix / "avoid redundant upgrades." Inaccurate (the second touch is a near-instant idempotent no-op) and would invite scope creep toward a brew-deps query.

## Tasks

<!-- Sequential work items for the apply stage. Checked off [x] as completed. -->

### Phase 1: Core Implementation

<!-- Reorder the shared Roster + its doc comment, and add the invariant test. These two
     are the source-of-truth change; the golden-test updates in Phase 2 depend on them. -->

- [x] T001 Reorder the `Roster` literal in `src/cmd/shll/tools.go` to leaves-first dependency order `wt, idea, tu, rk, hop, fab-kit`. Move entries only — each entry's `Name`, `Formula`, `ShellInit`, and `Update` fields stay byte-identical; do NOT add a `DependsOn` field or any field to the `Tool` struct. <!-- A-001, A-002, A-008 -->
- [x] T002 Extend the `Roster` doc comment in `src/cmd/shll/tools.go` to explain the leaves-first rationale: order is leaves-first so dependents are processed after their dependencies, keeping each tool's per-tool `shll update` / `shll install` output section complete before any dependent's internal `brew upgrade` can re-touch it. Preserve the existing shell-init-sequencing note. The comment MUST NOT claim the order changes upgrade *correctness* (brew owns that). <!-- A-003, A-011 -->
- [x] T003 Add `TestRosterLeavesBeforeDependents` to `src/cmd/shll/tools_test.go` (create the file if absent, `package main`). Encode the full dependency graph as data, each edge labeled by kind via comments: `fab-kit -> wt` `// brew-upgrade dep`, `fab-kit -> idea` `// brew-upgrade dep`, `hop -> wt` `// brew-upgrade dep` AND `// runtime-invocation dep` (hop open delegates to wt's menu; hop ls --trees fans out wt list --json), `rk -> wt` `// runtime-invocation dep` (rk riff shells out to wt create). Build a `name -> roster index` map from `Roster`, then assert for every edge `index[dependent] > index[dep]`; on violation fail with a message naming the offending edge and both indices (e.g. `"fab-kit (index N) must come after wt (index M)"`). Add a comment stating the test guards the toolkit's full ordering contract (brew-upgrade AND runtime edges) — a superset of output-coherence's strict need — so a reader does not infer that a runtime edge (e.g. `rk -> wt`) means `rk update` touches `wt` during `shll update`. <!-- A-004, A-005, A-006, A-009, A-010 -->

### Phase 2: Integration & Edge Cases

<!-- Conform the order-sensitive golden strings in the three consuming commands' tests to the
     re-declared Roster order (Test Integrity: tests conform to the spec/declared order, never
     the reverse). NO update.go / install.go / shell_init.go implementation changes. -->

- [x] T004 [P] Update the order-sensitive golden strings in `src/cmd/shll/update_test.go` to the new order. In `TestUpdate_HeadersAndTail`, change the per-tool header sequence from `==> fab-kit\n==> rk\n==> tu\n==> hop\n==> wt\n==> idea\n` to `==> wt\n==> idea\n==> tu\n==> rk\n==> hop\n==> fab-kit\n` (the `==> shll (self)` header and `Done — 7 of 7` tail are unchanged). Leave order-independent tests untouched: `TestUpdate_SelfUpgradeOrdering` / `TestUpdate_OneUpgradeFails` reference `Roster[0]` dynamically (now `wt`) and need no edit; `TestUpdate_PartialInstalled`, `TestUpdate_NoToolsInstalled` (the `No sahil87 tools installed.` golden), and other invariant tests do not depend on inter-tool order. No `update.go` change. <!-- A-007, A-012, A-013 -->
- [x] T005 [P] Update the order-sensitive golden string in `src/cmd/shll/install_test.go`. In `TestInstall_HeadersAndTail` (hop+wt installed → missing = {idea, tu, rk, fab-kit}), change the header sequence from `==> fab-kit\n==> rk\n==> tu\n==> idea\n` to the new roster order of the missing set: `==> idea\n==> tu\n==> rk\n==> fab-kit\n` (the `Done — 4 of 4` tail is unchanged). Leave order-independent tests untouched (`TestInstall_AllAlreadyInstalled` golden, set-membership assertions in `TestInstall_NoneInstalled` / `TestInstall_PartialInstalled`, count-based `TestInstall_OneInstallFails`). No `install.go` change. <!-- A-014, A-015 -->
- [x] T006 [P] Update the integrator concatenation order in `src/cmd/shll/shell_init_test.go` from `tu, hop, wt` to `wt, tu, hop` (integrators wt@0, tu@2, hop@4 in ascending `Roster` index order, as `runShellInit` iterates). Affected goldens: `TestShellInit_ZshAllIntegratorsInstalled` (the three `# ── <tool> ──` blocks), `TestShellInit_DeterministicOrder` (→ `# ── wt ──\nWT\n# ── tu ──\nTU\n# ── hop ──\nHOP\n`), and `TestShellInit_SubToolFailure` (the surviving-blocks golden becomes `# ── wt ──\nWT\n# ── tu ──\nTU\n` when hop is the failing/omitted block — re-confirm hop is the one with no separator). Each block's `# ── <tool> ──` separator text and the eval-safety/determinism invariants are unchanged; only inter-block order flips. No `shell_init.go` change. <!-- A-016, A-017, A-018 -->
- [x] T007 Verify `shll version` needs no golden change: confirm `src/cmd/shll/version_test.go` pairs assertions to `Roster` itself (`for i, tool := range Roster { ... lines[i+1] ... }`) so reorder moves expected and actual in lockstep. Make no edit if confirmed; if any hardcoded sequence is found, flag it. <!-- A-019 -->
- [x] T008 Run the project's test command (`go test ./...` from `src/`, or the justfile equivalent) and confirm all packages pass green, including the new `TestRosterLeavesBeforeDependents` and the updated golden strings. <!-- A-020 -->

## Execution Order

<!-- Included because the dependency is non-obvious and load-bearing: the golden-string
     test updates assert the NEW order, so they only pass once the reorder lands. -->

- T001 (reorder) MUST precede T004, T005, T006 — those tests assert the new order and would fail against the old `Roster`.
- T002, T003 touch the same file (`tools.go` / new `tools_test.go`) work area as T001 and are logically part of the same source-of-truth change; do them together in Phase 1.
- T004, T005, T006 are mutually independent ([P]) — three different test files, no shared edits.
- T007 (version verification) is independent of the golden updates but should land before T008.
- T008 (full test run) is the final gate — run after all edits.

## Acceptance

<!-- Declarative acceptance criteria used by the review stage. -->

### Functional Completeness

<!-- Every requirement in spec.md has working implementation. -->

- [x] A-001 Leaves-First Roster Order: the `Roster` literal in `src/cmd/shll/tools.go`, read top to bottom, is exactly `wt, idea, tu, rk, hop, fab-kit`. Verified at tools.go:64-71.
- [x] A-002 Per-entry fields preserved: each reordered entry's `Name`, `Formula`, `ShellInit`, and `Update` values are identical to their pre-change definitions; the `Tool` struct gains no field. Verified by `git diff` — only entry order + comment changed; struct untouched.
- [x] A-003 Rationale documented: the `Roster` doc comment explains the leaves-first dependency rationale (dependents processed after their dependencies) in addition to the existing shell-init sequencing note. Verified at tools.go:41-63.
- [x] A-004 Invariant test exists: `TestRosterLeavesBeforeDependents` is present in `src/cmd/shll/tools_test.go` and encodes the dependency graph as data with all four edges labeled by kind via comments. Verified at tools_test.go:24-35.
- [x] A-005 All edges asserted: the test builds a `name -> roster index` map and checks `index[dependent] > index[dep]` for every encoded edge (`fab-kit->wt`, `fab-kit->idea`, `hop->wt`, `rk->wt`). Verified at tools_test.go:39-56.
- [x] A-006 Edge-naming failure message: on violation the test fails with a message naming the offending edge and both indices. Verified at tools_test.go:54 (`"%s (index %d) must come after %s (index %d)"`).
- [x] A-007 update_test.go conforms: order-sensitive assertions in `src/cmd/shll/update_test.go` reflect `wt, idea, tu, rk, hop, fab-kit` (after the `shll (self)` step) with no `update.go` change. Verified at update_test.go:589; update.go unchanged in working tree.
- [x] A-014 install_test.go conforms: order-sensitive assertions in `src/cmd/shll/install_test.go` reflect the new leaves-first order with no `install.go` change. Verified at install_test.go:202 (`==> idea, ==> tu, ==> rk, ==> fab-kit`); install.go unchanged.
- [x] A-016 shell-init order is tu, wt, hop: `src/cmd/shll/shell_init_test.go` golden assertions reflect the `tu, wt, hop` concatenation order with no `shell_init.go` change. **SPEC-DRIFT NOTE**: the correct (code-derived) order is `wt, tu, hop` (wt@0, tu@2, hop@4 in ascending index order), NOT the spec's `tu, wt, hop`. The tests correctly assert `wt, tu, hop` per Test Integrity. Met on the substance (goldens conform to the real Roster order, no `shell_init.go` change); the spec/plan TEXT `tu, wt, hop` is the error. See adjudication.
- [x] A-019 version needs no change: `src/cmd/shll/version_test.go` passes unchanged, confirmed index-paired to `Roster` order rather than a hardcoded sequence. Verified at version_test.go:64-66 (`for i, tool := range Roster { ... lines[i+1] ... }`); not in working-tree diff.
- [x] A-020 All tests green: `go test ./...` passes for the whole module. Confirmed via `go test ./cmd/shll/ -v` — all PASS, including `TestRosterLeavesBeforeDependents`.

### Behavioral Correctness

<!-- Changed requirements behave as specified, not as before. -->

- [x] A-011 Comment does not over-claim: the `Roster` doc comment does NOT assert the reorder changes upgrade *correctness* (brew owns formula-dependency resolution) — it frames the order as output coherence / sequencing only. Verified at tools.go:51-53 ("This is OUTPUT COHERENCE, not a correctness fix").
- [x] A-012 Order-independent update invariants untouched: brew-missing bail, status line, single `brew update`, self-upgrade-before-roster, best-effort loop, summary tail, and exit codes are unchanged; the `No sahil87 tools installed.` empty-case golden remains verbatim. update.go not in working-tree diff; `TestUpdate_NoToolsInstalled`/`TestUpdate_EmptyCaseNoHeaderNoTail` goldens unchanged and PASS.
- [x] A-013 Dynamic-index update tests still pass: `TestUpdate_SelfUpgradeOrdering` and `TestUpdate_OneUpgradeFails` (which reference `Roster[0]`, now `wt`) pass without edits. Both reference `Roster[0]` dynamically (update_test.go:205, 391); no edit in diff; both PASS.
- [x] A-015 Install invariants untouched: `brew install` partitioning, the all-already-installed short-circuit golden, and partial/failure tails are unchanged apart from the reordered header sequence. Only line 202 (the `==>` header sequence) changed in install_test.go; all other install goldens unchanged and PASS.
- [x] A-017 shell-init eval-safety preserved: a missing or erroring integrator still emits neither its block nor its `# ── <tool> ──` separator; stdout stays eval-safe (no `▸`/`==>` header, no color) across the reorder. `TestShellInit_SubToolFailure`, `_OnlyTuInstalled`, `_NoIntegratingToolsInstalled` PASS; shell_init.go unchanged.
- [x] A-018 shell-init determinism preserved: `shll shell-init` output remains byte-identical across consecutive runs after the reorder. `TestShellInit_DeterministicOrder` PASS (asserts byte-identical across two runs, golden = `wt, tu, hop`).

### Scenario Coverage

<!-- Key scenarios from spec.md have been exercised. -->

- [x] A-009 Regression-fails-loudly scenario: a hypothetical reorder placing a dependent before one of its deps (e.g. `fab-kit` ahead of `wt`) would make `TestRosterLeavesBeforeDependents` fail with the offending edge named — verifiable by inspecting the assertion + message construction. Verified at tools_test.go:53-55 (`if dependentIdx <= depIdx { t.Errorf(...) }` with both indices in the message).
- [x] A-010 Reader-not-misled scenario: a comment on the `rk -> wt` runtime edge makes clear it reflects runtime invocation (`rk riff -> wt create`), not an `rk update`-time upgrade of `wt`, and that the contract is a superset of output-coherence's strict (brew-edge-only) need. Verified at tools_test.go:13-23 (superset/IMPORTANT-for-the-reader block) and tools_test.go:32-34 (per-edge rk->wt comment).

### Code Quality

<!-- Per fab/project/code-quality.md Principles + Anti-Patterns relevant to this change, plus baselines. -->

- [x] A-008 No magic strings / named constants: reordered entries continue to use `formulaPrefix` and `shellPlaceholder` named constants; no formula or shell token is open-coded by this change (code-quality: "magic strings ... tool roster entries belong in named constants"). Verified at tools.go:64-71 — every entry uses `formulaPrefix + "<name>"` and `shellPlaceholder`.
- [x] A-021 Dependency model stays implicit (project-specific review rule): the inter-tool dependency graph is expressed only as slice order + the invariant test — no `DependsOn` field, no runtime topological sort, no `brew deps` query (Constitution III/VII; Non-Goals). Verified — `Tool` struct (tools.go:11-34) gains no field; no `brew deps` anywhere; test reads slice order only.
- [x] A-022 Follow hop patterns: the new `tools_test.go` lives alongside source (test-alongside strategy), uses `package main`, and follows the surrounding table-driven / `Roster`-iterating test conventions of the existing `*_test.go` files. Verified — tools_test.go:1 `package main`; data-driven `[]rosterEdge` slice; iterates live `Roster` like version_test.go/shell_init_test.go.
- [x] A-023 No unnecessary duplication: the test derives its index map from the live `Roster` rather than re-listing tool names, and no per-tool shell/update logic is reimplemented in shll. Verified at tools_test.go:39-42 (`for i, tool := range Roster { indexByName[tool.Name] = i }`).
- [x] A-024 Pattern consistency: edited golden strings match the surrounding test style (literal `==>` / `# ── <tool> ──` framing, same quoting and `\n` concatenation) with no incidental reformatting. Verified by `git diff` — only the order tokens within the golden strings changed; quoting/concatenation style preserved.
