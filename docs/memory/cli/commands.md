---
type: memory
description: "Root command, subcommand wiring, exit-code sentinels (`errSilent`, `errExitCode`), version ldflags injection, and the hardcoded `Roster` slice."
---
# cli/commands

Top-level command surface for the `shll` binary — the cobra root, the subcommands it wires up, the exit-code translation layer, and the hardcoded tool roster every subcommand consumes.

## Binary entry point

`shll` is a single Go binary. The entry point is `src/cmd/shll/main.go:20`:

- The package-level `version = "dev"` (`src/cmd/shll/main.go:18`) is overridden via `-ldflags "-X main.version=<v>"` at build time. The literal default is `dev` (covers `go run` and unstamped local builds).
- `main()` builds the cobra root via `newRootCmd()`, sets `rootCmd.Version = version`, executes, and translates any `RunE` error to an exit code via `translateExit`.

## Cobra root

`newRootCmd()` (`src/cmd/shll/root.go:19`) returns the cobra command with:

- `Use: "shll"`
- `Short: "meta-CLI for the sahil87 toolkit"`
- A `Long` block (`rootLong`, `src/cmd/shll/root.go`) listing the twelve user-facing subcommands and noting that per-tool CLIs continue to work standalone.
- `SilenceUsage: true` and `SilenceErrors: true` — usage is not printed on RunE errors, and cobra's default error printer is suppressed. The `translateExit` layer in `main.go` owns stderr.
- `AddCommand` (`src/cmd/shll/root.go`) wires **13 factory funcs**: `newDoctorCmd()`, `newInstallCmd()`, `newUpdateCmd()`, `newUninstallCmd()` (change kkaj), `newChangelogCmd()` (change r01z), `newShellInitCmd()`, `newShellSetupCmd()`, `newVersionCmd()`, `newListCmd()`, `newStandardsCmd()` (change vo8c), `newSkillCmd()` (change agst), `newAgentSetupCmd()` (change agst), and the hidden `newHelpDumpCmd()`. The hidden `help-dump` is not counted in the user-facing surface (see [cli/help-dump-contract](/cli/help-dump-contract.md)), so the **user-facing surface is twelve**.

Per Constitution VII (Minimal Surface Area), every top-level subcommand requires explicit justification in the change's intake. The current user-facing surface is twelve subcommands (the hidden `help-dump` is not counted — see [cli/help-dump-contract](/cli/help-dump-contract.md)).

### Constitution VII justification per subcommand

These were locked in at spec time (Design Decision #1 for the original three; `install`, `shell-setup` (formerly `shell-install`), `doctor`, `changelog`, `uninstall`, `standards`, `skill`, and `agent-setup` were added later, each with its own Constitution VII justification — see [cli/install](/cli/install.md#constitution-vii-justification), the `shell-setup` bullet below, [cli/doctor](/cli/doctor.md), [cli/changelog](/cli/changelog.md#constitution-vii-justification), the `uninstall`, `standards`, `skill`, and `agent-setup` bullets below, [cli/standards](/cli/standards.md#constitution-vii-justification), [cli/skill](/cli/skill.md#constitution-vii-justification), and [cli/agent-setup](/cli/agent-setup.md#constitution-vii-justification)) and are reproduced here:

- **`skill`** (change agst) — solves the agent-usage-knowledge gap: an agent driving a toolkit tool has no offline, version-locked "when to reach for which" briefing (`-h`/help-dump is flag reference; README/docs-site needs a checkout or network). `skill` composes each installed tool's own `<tool> skill` bundle at runtime — bare = a one-line-per-installed-tool glossary, `shll skill <tool>` = that tool's full bundle passed through byte-identical (`shll skill shll` = shll's own embedded bundle). Cannot be a flag on an existing subcommand: none reads or composes per-tool agent bundles, and the self-target embed needs special-casing (a subprocess self-invocation would recurse into the composer). Not a per-tool concern (Constitution IV) — cross-tool bundle aggregation is exactly the meta-tool's job. It is verbatim the `shell-init` composition pattern Constitution III/IV exist to bless. See [cli/skill](/cli/skill.md).
- **`agent-setup`** (change agst) — solves the harness-wiring-is-mis-homed gap: the cross-toolkit machine wiring that points agent harnesses at the toolkit lived in `run-kit agent-setup`, but run-kit is a leaf tool and a static stanza cannot scale to per-tool bundles — it belongs in the manager (shll). `shll agent-setup` mechanically places one thin `shll-toolkit` Agent Skill at two global skill paths, then delegates run-kit's dashboard hooks back to `run-kit agent-setup`. Cannot be a flag on any existing subcommand (a distinct machine-provisioning verb). It graduated the two former `run-kit agent-setup` pointers (the [install nudge](/cli/install.md#the-post-install-next-steps-nudge-change-93r2) + README). See [cli/agent-setup](/cli/agent-setup.md).
- **`standards`** (change vo8c) — solves the standards-reachability gap: the toolkit gained binding, producer-facing standards (the ten CLI principles, the help-dump contract, the README/docs-site structure standard), but no way for an AI agent working in *any* toolkit repo to read them offline and without prior knowledge of where they live. Serving toolkit-wide standards is a cross-toolkit concern — exactly the meta-tool's job. Cannot be a flag on an existing subcommand — no existing subcommand reads or prints documents (`list` is the tool roster, `help-dump` is the machine help contract, `version` is a frozen bug-report table). Cannot live in a per-tool CLI — the standards govern all seven tools, so no single tool owns them (Constitution IV). The bare list form is a self-describing glossary (an agent told only "run `shll standards`" can pick the right document); `shll standards <name>` prints the canonical `docs/site/<name>.md` byte-identical, embedded at build time (offline, versioned with the release). See [cli/standards](/cli/standards.md).
- **`uninstall`** (change kkaj) — solves the no-clean-slate-path gap: when brew state gets wedged (an unlinked keg, a dual-rack orphan, a half-failed upgrade), the only recovery was hand-typed brew surgery across up to seven formulas — `shll install` bootstraps but nothing removes. `uninstall` is a distinct **destructive** verb, not a variant of an existing operation: hiding it behind a flag on `install` (e.g. `--clean`) would bury a destructive action inside an additive command, and a per-tool CLI cannot own it (removing *all* toolkit members is inherently the meta-tool's job). It is also the escape hatch the [rk→run-kit migration guard](/cli/update.md#the-rkrun-kit-migration-guard-change-9bak) assumes exists — its dual-rack handling is deliberately detect-and-note-only, deferring cleanup to `shll uninstall run-kit`. See [cli/uninstall](/cli/uninstall.md).
- **`changelog`** (change r01z) — solves the update-opacity gap: `shll update` upgrades up to seven tools but the record of *what changed* is buried in streamed output. Cannot be a flag on `update` — its core purpose is *deterministic re-display after the fact* (re-read notes without re-running an upgrade) and it has standalone value independent of updating (pre-update preview, historical range queries). Not a per-tool concern — its job is cross-toolkit release-notes aggregation, exactly shll's mandate. `shll update` *composes* it (printing a compact digest + the re-runnable `shll changelog ...` command), but the primitive is the standalone command. See [cli/changelog](/cli/changelog.md).
- **`doctor`** (change d0ct) — solves the post-install verification gap referenced by the shll.ai docs (the install + idea pages name `shll doctor` as *the* verification step, but the command did not exist). Cannot be a flag on `version` — verification is a different concern (wiring checks, OK/WARN/FAIL semantics, a CI-meaningful non-zero exit) from version *reporting* (a plain table that always exits 0). Cannot be a flag on `install`/`update` — those *mutate*, whereas a doctor must be strictly read-only. Cannot live in a per-tool CLI — its value is the cross-tool aggregation plus the shll-owned rc-block wiring check (no single tool can see whether the *composed* shll block is present). Its `--json` is a flag on `doctor`, not a second subcommand. See [cli/doctor](/cli/doctor.md) for the full behavior contract.
- **`install`** — solves the bootstrap pain (a new user wants "get me the toolkit"). Distinct lifecycle from `update`: different precondition (not-installed vs. installed), different failure modes, different discoverability. Cannot cleanly fold into `update --install-missing` without inverting that command's installed-only precondition. See [cli/install](/cli/install.md) for the full justification and behavior.
- **`update`** — solves the no-single-update-command pain (`brew upgrade sahil87/tap/all` does NOT propagate to deps). Cannot be a flag on an existing tool because the entry point itself is what's missing. Also self-upgrades shll itself when shll was brew-installed — see [cli/update](/cli/update.md#shll-self-upgrade).
- **`shell-init`** — solves the cold-start cost and N-eval-line burden when multiple shell-integrating tools are installed. Per-tool `shell-init` keeps working standalone (Constitution IV).
- **`shell-setup`** (canonical name since change ri3h; `shell-install` retained as a back-compat alias) — solves the manual-rc-edit cliff in the post-`brew install` onboarding flow. Cannot be a flag on `shell-init` (it *invokes* `shell-init`, so making it a sub-flag is structurally self-referential). Cannot live in a per-tool CLI (per-tool CLIs emit their own shell-init; this command writes the cross-tool composition `eval "$(shll shell-init <shell>)"`, which is exactly what shll exists for). The `shell-setup` rename adds the alias without changing the surface-area count (five at the time of change ri3h; seven now that changes lst7 and d0ct added `list` and `doctor`). It briefly carried a `--trust-tap` flag (change l6lo) for whole-tap Homebrew trust, but change 0854 **removed it** — `shell-setup` is now pure rc-wiring, and per-formula trust moved to `shll install` (a net surface-area reduction) — see [cli/shell-setup](/cli/shell-setup.md).
- **`version`** — solves the bug-report triage pain. Cannot live on a per-tool CLI because the value is the cross-tool aggregation.
- **`list`** (change lst7) — solves the toolkit-discovery gap (the shll.ai homepage advertised a `shll list` mock for a command that did not exist). Cannot fold into `version`: `version` is deliberately frozen as plain-text-only, no-JSON, versions-only output for bug reports (version Design Decision #4), whereas `list` aggregates roster *metadata* (one-line descriptions + repo links) and needs structured `--json` — bolting that on would either break version's contract or be `list` in disguise. Not a per-tool concern (Constitution IV) — the value is the cross-tool aggregation. The roster it iterates is single-sourced with what `install`/`update`/`version`/`shell-init` consume, so it cannot drift from what's actually managed. See [cli/list](/cli/list.md#constitution-vii-justification) for the full justification and behavior.

## Exit-code translation

`translateExit(err error) int` in `src/cmd/shll/main.go` is the single mapping from `RunE` (and cobra-internal) errors to OS exit codes. It implements the **toolkit convention** — `0` success / `1` operational failure / `2` usage error (bad invocation) — stated by the principles standard, and shll's conformance to it is the audited receipt for principle №4 (see [cli/standards-conformance §usage errors exit 2](/cli/standards-conformance.md#1-usage-errors-exit-2-principle-4)). It uses two error sentinels defined in `src/cmd/shll/main.go`:

- `errSilent = errors.New("shll: silent error")` — returned by subcommands that have already written their own diagnostic to stderr. Maps to exit code 1; `translateExit` does not write anything else.
- `errExitCode{code, msg}` — used when a subcommand (or the root flag-error hook) needs an exit code other than the default. `shll shell-init` and `shll shell-setup` construct it directly (still `errExitCode{code: 2, msg: …}` literals — NOT the `usageExitCode` constant) for their bad/missing-shell and related user-invocation errors (missing rc file, mutually-exclusive flags, corrupted block); the root `SetFlagErrorFunc` constructs it with `usageExitCode`. If `msg` is non-empty, `translateExit` writes it to stderr. It is matched via `errors.As`, so a wrapped `errExitCode` still yields its code (`TestTranslateExit_WrappedErrExitCode`).

### The `0/1/2` classification (change 3sss)

`translateExit` classifies in this order — the first match wins:

1. **`errExitCode{...}`** (via `errors.As`) → its explicit `code`. This is how the exit-2 *usage* cases that flow through a typed path arrive: the root `SetFlagErrorFunc` (below) wraps flag-parse errors as `errExitCode{code: usageExitCode}`, and `shell-init`/`shell-setup` wrap their own usage errors as code 2.
2. **`errSilent`** (via `errors.Is`) → `1` (operational failure; the caller already wrote its diagnostic).
3. **A cobra *arg/command* usage error** → `usageExitCode` (2). Cobra v1.10.2 exposes a clean hook only for *flag* errors; the arg/command usage errors it raises outside flag parsing (unknown command, invalid argument, wrong arg count) are plain `fmt.Errorf` values with **no typed sentinel**, so they are classified by their **stable message prefixes** via `isUsageError(err) bool` against the `cobraUsageErrorPrefixes` slice (`"unknown command "`, `"unknown flag: "`, `"unknown shorthand flag: "`, `"invalid argument "`, `"accepts "`, `"requires "`). Classification is **prefix-anchored** (`strings.HasPrefix`), not a loose substring search, so an operational error whose message merely *contains* a usage word mid-string stays exit 1. The root silences cobra's own usage/error printing, so `translateExit` writes the diagnostic to stderr itself here.
4. **Anything else** → `1` (operational failure); printed to stderr.

`usageExitCode = 2` is a **named constant** in `main.go` (code-quality.md: no magic numbers), single-sourced between `translateExit` and `root.go`'s `SetFlagErrorFunc`. The flag-error prefixes are absent from `cobraUsageErrorPrefixes` on purpose — flag errors are already wrapped into `errExitCode{code: 2}` by the hook before `Execute` returns — but including flag-error prefixes there would be harmless defense-in-depth (the code comment notes this).

### The root `SetFlagErrorFunc` (change 3sss)

`newRootCmd()` (`root.go`) installs `cmd.SetFlagErrorFunc(func(_ *cobra.Command, err error) error { return &errExitCode{code: usageExitCode, msg: err.Error()} })`. Set on the **root** so every subcommand inherits it (cobra walks up to the root when a command sets no `FlagErrorFunc` of its own). This is the clean typed hook for flag-parse errors (unknown flag, bad flag value); the arg/command usage errors that carry no such hook are the ones `translateExit` classifies by prefix (above). Pinned by `TestRootCmd_FlagErrorIsUsageExit` (drives `root.Execute()` with a bad flag and asserts the error is an `errExitCode{code: usageExitCode}`).

The exit-code contract as a whole is pinned by `src/cmd/shll/main_test.go` (`TestTranslateExit_Contract` — the sentinel paths, the six cobra usage-error prefixes → 2, the mid-message operational error → 1; `TestTranslateExit_WrappedErrExitCode`; `TestRootCmd_FlagErrorIsUsageExit`).

Default fallback: any other error is printed to stderr and exits 1.

This layered design keeps cobra's own error printing out of the way (`SilenceErrors: true`) and concentrates exit-code policy in one place. The hop binary uses the same pattern; shll mirrors it.

## Subcommand factory pattern

Every subcommand follows `newXxxCmd()` returning `*cobra.Command` (no globals, no init() side effects). The cobra command's `RunE` calls a thin top-level helper (`runDoctor`, `runUpdate`, `runChangelog`, `runShellInit`, `runShellSetup`, `runVersion`, `runList`) that takes explicit `io.Writer` arguments — this is the test seam: tests drive these directly with `bytes.Buffer` writers and a fake `proc.Runner` (or, for `shell-setup`, no fake — the command does file I/O only). `runChangelog(ctx, stdout, stderr, args)` (change r01z) additionally drives the [internal/changelog](/internal/changelog.md) `SetTransportForTest` seam to point release fetches at an `httptest.Server`. `runList(ctx, stdout, jsonOut bool)` adds a `bool` flag arg alongside the writer (the `--json` selector). `runDoctor` additionally takes an `env func(string) string` seam (production passes `os.Getenv`) so the wiring check reads a `t.TempDir()` rc file via a map-backed env — mirroring `resolveShell`/`resolveRcFile`.

## Hardcoded tool roster

Defined in `src/cmd/shll/tools.go`. Constitution III (Tool Roster Source of Truth) requires this to be hardcoded and versioned with the binary — there is NO runtime discovery (no `brew tap` parsing, no filesystem walk).

The `Tool` struct carries eight fields: `Name`, `Formula`, `ShellInit`, `Update`, `Description` and `Repo` (added by change lst7), and `LegacyName`/`LegacyFormula` (added by change 9bak — transitional, see [The rk→run-kit rename + migration fields](#the-rkrun-kit-rename--migration-fields-change-9bak)). Each `Roster` entry populates all the fields its capabilities require:

```go
var Roster = []Tool{
    {Name: "wt",      Formula: "sahil87/tap/wt",      ShellInit: []string{"wt", "shell-init", "<shell>"},  Update: []string{"wt", "update"},      Repo: "wt",      Description: "Git worktree management — create, list, open, delete worktrees"},
    {Name: "idea",    Formula: "sahil87/tap/idea",                                                         Update: []string{"idea", "update"},    Repo: "idea",    Description: "Backlog idea management from the terminal"},
    {Name: "tu",      Formula: "sahil87/tap/tu",      ShellInit: []string{"tu", "shell-init", "<shell>"},  Update: []string{"tu", "update"},      Repo: "tu",      Description: "Token-usage tracker for AI coding tools (Claude Code, Codex, OpenCode)"},
    {Name: "run-kit", Formula: "sahil87/tap/run-kit",                                                      Update: []string{"run-kit", "update"}, Repo: "run-kit", LegacyName: "rk", LegacyFormula: "sahil87/tap/rk", Description: "Run-kit — tmux session manager with a web UI (rk stays as an alias)"},
    {Name: "hop",     Formula: "sahil87/tap/hop",     ShellInit: []string{"hop", "shell-init", "<shell>"}, Update: []string{"hop", "update"},     Repo: "hop",     Description: "Fast directory/project jumping across worktrees"},
    {Name: "fab-kit", Formula: "sahil87/tap/fab-kit",                                                      Update: []string{"fab-kit", "update"}, Repo: "fab-kit", Description: "Spec-driven workspace & workflow toolkit (the `fab` CLI)"},
}
```

Roster invariants:

- **Order matters — leaves-first dependency order (change auvj).** The slice is declared `wt, idea, tu, run-kit, hop, fab-kit`: every tool appears *after* all of its dependencies. The leaves `wt, idea, tu` (no outgoing edges) precede the dependents `run-kit, hop, fab-kit`. `shll shell-init` concatenates output in roster order, `shll update`/`shll install` probe/upgrade/install in roster order, and `shll version` prints rows in roster order — so the single declared order drives all four consumers. **Why leaves-first**: it is *output coherence*, not a correctness fix (brew already resolves formula dependencies correctly and idempotently, and each `<tool> update` is self-update-only, so the order can neither break nor improve upgrade correctness). What it buys is that each tool's per-tool output section in `shll update` / `shll install` completes (and is counted under its own `▸ <tool>`/`==> <tool>` header) before a dependent's internal `brew upgrade` can re-touch a leaf already reported done. The invariant is enforced by `TestRosterLeavesBeforeDependents` (`src/cmd/shll/tools_test.go`) — a comment cannot fail CI, so the test guards against an accidental re-alphabetize or reorder and names the offending edge with both indices on violation. The `run-kit -> wt` runtime edge (was `rk -> wt` before change 9bak) is one of the encoded edges. See the [leaves-first ordering Design Decision](#design-decision-leaves-first-roster-order-change-auvj) below.
- **Six tools.** Adding a tool is a `shll` release, not a runtime configuration change.
- **`Tool.ShellInit`** is the argv of the tool's shell-init invocation. Empty slice = no shell integration. The literal token `<shell>` (declared as `shellPlaceholder` in `src/cmd/shll/tools.go:65`) is substituted with the user-supplied shell name (`zsh`/`bash`) at composition time. All three integrators (`tu`, `hop`, `wt`) substitute the placeholder uniformly — three of the six roster entries carry shell integration.
- **`Tool.Update`** (added in change cczs) is the argv of the tool's own `update` invocation, mirroring `ShellInit`'s "empty slice means no capability" semantics. `shll update` delegates to this argv (appending `--skip-brew-update` when the tool advertises it) instead of calling `brew upgrade <formula>` directly, so each tool's post-upgrade side effects (e.g. run-kit's daemon restart) are preserved (Constitution IV). An empty slice means the tool exposes no `update` subcommand → `shll update` falls back to `brew upgrade <formula>`. **All six current roster entries populate `Update`** (`{"<name>", "update"}` — run-kit's is `{"run-kit", "update"}` since change 9bak) — every sahil87 tool ships an `update` subcommand. See [cli/update](/cli/update.md#behavior-contract) for the delegation/probe logic. *(A `needsMigration` run-kit is the one exception: on a legacy-keg machine it is migrated brew-direct rather than via its `Update` argv — see [cli/update §migration guard](/cli/update.md#the-rkrun-kit-migration-guard-change-9bak).)*
- **`Tool.Description`** (added in change lst7) is a one-line, human-readable summary printed by `shll list`. Single-sourced on the roster so it cannot drift from the managed set (Constitution III). All six entries populate it (guarded by `TestList_RosterFieldsNonEmpty`).
- **`Tool.Repo`** (added in change lst7) is the `github.com/sahil87/<Repo>` slug for the tool's source repository, used by `shll list` to render the repo column. **It defaults to `Name`, and since change 9bak `Name == Repo` for every roster entry.** Historically it was NOT always equal (the pre-rename tool `rk` had repo `run-kit` — `github.com/sahil87/rk` was a 404); the rk→run-kit rename made the identity match, so the field no longer *diverges* on any current tool. It stays explicit (not derived from `Name`) as future-proofing so `shll list` never emits a dead link if a future tool's binary name and repo slug diverge again. The single URL-composition point is `repoURL(t)` (`src/cmd/shll/list.go`) = `githubOrgBase + t.Repo`, guarded by `TestList_RepoLinks` (still asserts run-kit resolves to `.../run-kit` and the dead `.../rk` link is absent). See [cli/list §The run-kit repo-slug footgun](/cli/list.md#the-run-kit-repo-slug-footgun-retired-by-change-9bak).
- **`formulaPrefix = "sahil87/tap/"`** (`src/cmd/shll/tools.go:11`) is a named constant — no magic string at the call sites.
- **`githubOrgBase = "https://github.com/sahil87/"`** (`src/cmd/shll/tools.go:78`, added by change lst7) is the named constant for the GitHub org base URL — `shll list` never open-codes the URL prefix (code-quality.md). A tool's repo URL is `githubOrgBase + tool.Repo`.

## The rk→run-kit rename + migration fields (change 9bak)

run-kit v3.0.0 completed a binary/formula rename: the tool that was `rk` is now `run-kit` (the tap installs the binary as `run-kit` with `rk` kept as a symlink alias; `formula_renames.json` maps `{"rk": "run-kit"}`, and `Formula/rk.rb` is deleted). Change 9bak brought shll's roster into line and added a **transitional migration guard** so a machine still holding the legacy `rk` keg upgrades cleanly. The roster changes are:

- **The roster entry `rk` became `run-kit`** — `Name: "run-kit"`, `Formula: "sahil87/tap/run-kit"`, `Update: {"run-kit", "update"}`, `Repo: "run-kit"` (now `Name == Repo`), at its **unchanged 4th position** (between `tu` and `hop`). The display name is now `run-kit` everywhere — `list`/`version`/`doctor` rows, the update digest, and JSON output — with no transitional dual display. Invocation compatibility is covered by the alias (below), not a second display identity.
- **Two new `Tool` fields, both transitional and empty for every tool except run-kit:**
  - **`LegacyName string`** (`"rk"`) — the tool's PRIOR binary name, used ONLY as a display-surface PATH-probe fallback: when `<Name> --version` returns `proc.ErrNotFound`, `probeToolVersion` retries `<LegacyName> --version` so a pre-rename install (whose binary is still `rk` on PATH) is shown installed by `list`/`version`/`doctor`. See [cli/version §the legacy-name PATH-probe fallback](/cli/version.md#the-legacy-name-path-probe-fallback-change-9bak).
  - **`LegacyFormula string`** (`"sahil87/tap/rk"`) — the tool's PRIOR fully-qualified brew formula, used ONLY by the update/install migration guard: when the current `Formula` is not installed but `LegacyFormula`'s keg is present (classified by keg **leaf name**), the tool is a pre-rename install to migrate brew-direct. See [cli/update §the rk→run-kit migration guard](/cli/update.md#the-rkrun-kit-migration-guard-change-9bak).
- **Both fields are sunset-marked in code** — once legacy `rk`-only installs die out, the PATH fallback and the migration gate become cheap no-ops and can be retired. The `Tool` struct itself keeps `Repo` (now redundant with `Name`) as documented future-proofing.

### The legacy target alias (`rk` → `run-kit`)

So `shll update rk` / `shll install rk` (muscle memory, existing scripts) keep working, a package-level map `legacyAliases = map[string]string{"rk": "run-kit"}` (`src/cmd/shll/tools.go`) is consulted inside `resolveTargets` **before** `rosterHas` — an aliased arg resolves to its canonical Roster tool. The mechanism:

- **`resolveTargets(args, allowShll) (selected []Tool, selfSelected bool, aliased []string, err error)`** (change 9bak added the `aliased []string` return). It stays **IO-free** (no brew/subprocess calls); when an arg matches a `legacyAliases` key whose canonical name is in the roster, it records the alias token in `aliased` (in encounter order) so the *caller* can print the notice. `resolveTargets` never prints — IO is the caller's concern.
- **`printAliasNotices(stdout, aliased)`** (`tools.go`) writes one `note: rk is now run-kit` line per aliased token (format constant `aliasNoticeFmt = "note: %s is now %s"`), to **stdout** (a friendly note, not an error). Shared by `update` and `install` so the wording is single-sourced; `changelog` gets the alias for free because `parseChangelogSpecs` routes names through the same `resolveTargets` (though changelog does not print the notice — it re-derives its own ordering).
- **Aliases are NEVER advertised in the valid-targets error diagnostic.** `validTargets(allowShll)` derives from the live `Roster` (+ `shll` when `allowShll`), so `shll update foo` errors `unknown target "foo" (valid targets: shll, wt, idea, tu, run-kit, hop, fab-kit)` — no `rk`. Accepted-but-unadvertised is the deliberate contract.

The alias is **transitional** — a documented sunset point alongside `LegacyName`/`LegacyFormula`.

### The keg-leaf parser (`parseBrewLeaf` / `probeInstalledLeaf`) — `brew.go`

The migration gate classifies by the keg **leaf name** parsed from `brew list --formula --versions` stdout (the first whitespace field of the first non-empty line, e.g. `rk` from `rk 2.5.13` vs `run-kit` from `run-kit 3.0.0`) — never by the exit code alone. Change 9bak extended `brew.go`:

- **`probeInstalledLeaf(ctx, formula) (installed bool, leaf, version string)`** is now the **sole** `brew list --formula --versions` invocation in `cmd/shll`. It returns the exit-code install fact, the leaf, and the parsed version from one read.
- **`probeInstalledVersion`/`isInstalled`/`installedVersion` are now thin wrappers** over `probeInstalledLeaf` (dropping the leaf), so there is still exactly one `brew list` invocation and existing boolean/version-only callers are untouched.
- **`parseBrewLeaf(out string) string`** is the leaf extractor (first `strings.Fields` token, `""` on empty) — whitespace split, never a regex (code-quality.md anti-pattern), mirroring `parseBrewVersion`.

**Why the leaf, not the exit code:** after the tap rename, `brew list --formula --versions sahil87/tap/rk` can EXIT 0 yet report leaf `run-kit` (rename-resolution — the migrated case, observed state B). Only the leaf distinguishes a genuine legacy `rk` keg from a migrated `run-kit` keg. Full gate + action: [cli/update §the rk→run-kit migration guard](/cli/update.md#the-rkrun-kit-migration-guard-change-9bak).

## The shared `shllSelf` descriptor (change bb7r)

`shll` is the manager-member of the toolkit, but it is **deliberately NOT in `Roster`** — `Roster` is the *managed sub-tool* list (Constitution III). To represent shll uniformly across the commands that show the toolkit, change bb7r added a **single shared descriptor** in `src/cmd/shll/tools.go` — the one source of truth for "shll as a displayable entry":

```go
const shllSelfDescription = "the manager for the shll toolkit"

var shllSelf = Tool{
    Name:        shllTargetToken, // "shll"
    Description:  shllSelfDescription,
    Repo:        shllTargetToken, // → https://github.com/sahil87/shll via repoURL
}

func shllSelfVersion() string { return normalizeVersion(version) } // package var, never `shll --version`
```

- **Reuses the `Tool` struct shape, but is not a `Roster` entry.** Only `Name`, `Description`, and `Repo` are populated. shll has no managed `Formula`, no own `ShellInit` to compose, and is self-upgraded via `update.go`'s dedicated path rather than a `Roster` `Update` argv. **Why not add shll to `Roster`** (the rejected alternative): it would violate Constitution III, break the leaves-first invariant guarded by `TestRosterLeavesBeforeDependents`, and make `install`/`update`/`shell-init` try to operate on the running orchestrator itself (e.g. `brew install` the live binary). `len(Roster)` stays **6**; `TestShllSelf_NotInRoster` guards `len(Roster) == 6` and `!rosterHas("shll")`.
- **Version from the package var, never a self-subprocess.** `shllSelfVersion()` returns `normalizeVersion(version)` (the same package-level `version` var `shll version`'s first row reads), so shll's version display is consistent across surfaces and no `shll --version` self-spawn is ever issued (shll is always present — it is the running process). `TestShllSelfVersion_FromPackageVar` pins this; `TestShllSelf_Descriptor` pins the field contract (`Name`/`Description`/`Repo`, and that `Formula`/`ShellInit`/`Update` are empty).

### Unified shll-first ordering — the principle

Every command that meaningfully shows the toolkit renders **shll FIRST, then the leaves-first `Roster`** (`shll, wt, idea, tu, run-kit, hop, fab-kit`):

| Command | shll-first instance | bb7r code change? |
|---------|---------------------|-------------------|
| `version` | leads with a `shll` row (version from the package var) | **No** — already shll-first |
| `update` | leads with the `shll (self)` step as `[1/M]` | **No** — already shll-first |
| `list` | prepends a shll-first table row (plain installed marker) + a `--json` object carrying `self:true` (`omitempty`) | **Yes** ([reverses lst7's "no self-row"](/cli/list.md#why-a-self-row--and-why-this-reverses-change-lst7)) |
| `doctor` | prepends an always-OK shll-first row/object (checks 1+2 only, `shell_init:false`, never touches `anyFail`, excluded from the problem-count denominator) | **Yes** ([cli/doctor](/cli/doctor.md#the-prepended-shll-first-row-change-bb7r)) |
| `install` | prepends a shll-first **informational** line (`"shll — already present / self-managed"`) — never a brew action | **Yes** ([cli/install](/cli/install.md#the-prepended-shll-first-informational-line-change-bb7r)) |
| `shell-init` | **excluded** — the one documented exception | n/a |

`version` and `update` were **already** shll-first; change bb7r introduced `shllSelf`/`shllSelfVersion()` to *generalize* that pattern to the inspect/manage surface (`list`/`doctor`/`install`). `version.go` and `update.go` were **not** changed — they keep their own inline shll-first handling, agreeing with `shllSelf` by construction (same package version var). `list`/`doctor`/`install` consume `shllSelf` for the displayable Name/Description/Repo.

**`shell-init` is the one deliberate exception.** It is the only toolkit-showing command that does NOT prepend a shll entry: shll has no shell-init output of its own to compose, and `shell-init`'s stdout is `eval`'d, so a stray shll line risks breaking the user's shell (Constitution V eval-safety). See [cli/shell-init §the deliberate exception](/cli/shell-init.md#the-deliberate-exception--do-not-unify-onto-the--header).

**The lst7 "no self-row" reversal is a display decision, not a Constitution rule.** Change lst7 had documented "No `shll` self-row" in `list`; bb7r reverses it **for discoverability** (a newcomer should see shll as part of the family). The reversal is recorded in exactly three live locations — `docs/memory/cli/list.md`, the `runList` doc comment in `list.go`, and `README.md` — and is **not** promoted to a constitutional rule (Constitution VII's new-subcommand bar is not triggered: bb7r adds *behavior* to existing commands, not a new subcommand).

### Design Decision: Leaves-first Roster order (change auvj)

The `Roster` is declared in **leaves-first dependency order** — every tool that depends on another (by brew-upgrade *or* by runtime invocation) appears *after* all of its dependencies. The order is `wt, idea, tu, run-kit, hop, fab-kit`.

The dependency edges driving this order (encoded as data in `TestRosterLeavesBeforeDependents`, each labeled by kind):

| Dependent → dep | Kind | Cause |
|-----------------|------|-------|
| `fab-kit → wt` | brew-upgrade | fab-kit's brew formula upgrades wt |
| `fab-kit → idea` | brew-upgrade | fab-kit's brew formula upgrades idea |
| `hop → wt` | brew-upgrade **and** runtime-invocation | hop's formula upgrades wt; `hop open` delegates to wt's menu and `hop ls --trees` fans out `wt list --json` |
| `run-kit → wt` | runtime-invocation | `run-kit riff` shells out to `wt create` (the edge was `rk → wt` before the change-9bak rename) |

So the leaves `wt, idea, tu` (no outgoing edges) precede the dependents `run-kit, hop, fab-kit`; `fab-kit` is a pure dependent (no cycle).

- **Why this ordering exists**: *output coherence* in `shll update` / `shll install`, **not** correctness. Brew owns formula-dependency resolution (correct and idempotent), and each `<tool> update` is self-update-only — no tool's `update` cascades into another tool's upgrade during `shll update`. The only observable effect of the old order (`fab-kit, rk, tu, hop, wt, idea`) was that a dependent's *internal* `brew upgrade` could re-touch a leaf already reported done under its own `▸ <tool>`/`==> <tool>` header (an idempotent near-instant no-op), under-representing the per-tool framing introduced by change y630. Leaves-first keeps each tool's section complete and counted before any dependent runs.
- **Why a test, not just a comment**: a comment cannot fail CI. `TestRosterLeavesBeforeDependents` (`src/cmd/shll/tools_test.go`) builds a `name → roster index` map from the live `Roster` and asserts `index[dependent] > index[dep]` for every edge, failing with the offending edge and both indices named (e.g. `"fab-kit (index N) must come after wt (index M)"`). It encodes **both** brew-upgrade and runtime-invocation edges — a *superset* of what output-coherence strictly needs (which depends only on the brew-upgrade edges) — so the executable contract documents the toolkit's full ordering relationship. A test comment makes clear a runtime edge (e.g. `run-kit → wt`) does **not** mean `run-kit update` touches `wt` during `shll update`. (Change 9bak updated this edge's `dependent` from `rk` to `run-kit`.)
- **Why the dependency model stays implicit (Constitution III/VII)**: the `Tool` struct gains **no** `DependsOn` field, and there is no runtime topological sort or `brew deps` query. shll's contract is the hardcoded roster *list* (Constitution III); it does not own a data model of how the tools relate — brew owns the brew graph and runtime edges are the sub-tools' concern. The dependency graph lives only as slice order + the invariant test. Rejected: (a) a `DependsOn []string` field with runtime topo-sort (models the inter-tool graph as shll-owned data, more code/failure modes); (b) a runtime `brew deps` query (more brew coupling, latency, runtime discovery the constitution discourages for roster concerns).
- **Why the shared `Roster`, not an update-only iteration order**: one source of truth. Reordering the single shared slice lets `update`, `install`, `shell-init`, and `version` all inherit the order. Rejected: a second, `update`-scoped iteration order (sorting at iteration time in `update.go`) — adds a divergence risk and a second ordering concept for marginal isolation benefit.

Traceability: change `260601-auvj-reorder-roster-leaves-first`. Output coherence is an `update`/`install` concern — see [cli/update](/cli/update.md#leaves-first-roster-order-change-auvj).

## File layout (src/cmd/shll/)

| File | Role |
|------|------|
| `main.go` *(extended by 3sss)* | Entry point, version variable, `translateExit`, `errSilent`, `errExitCode`. Change 3sss added the `0/1/2` usage-error classification: `usageExitCode = 2` (named constant), `cobraUsageErrorPrefixes` (the stable arg/command usage-error prefixes) and `isUsageError` (prefix-anchored classifier) — see [exit-code translation](#exit-code-translation). |
| `root.go` *(extended by 3sss, agst)* | `newRootCmd()` — cobra root that `AddCommand`s 13 factory funcs (12 user-facing — `doctor`, `install`, `update`, `uninstall` (change kkaj), `changelog`, `shell-init`, `shell-setup`, `version`, `list`, `standards` (change vo8c), `skill` (change agst), `agent-setup` (change agst) — plus the hidden `help-dump`); also holds the `rootLong` help text listing the twelve user-facing subcommands. Change 3sss added a root `SetFlagErrorFunc` wrapping flag-parse errors in `errExitCode{code: usageExitCode}` (inherited by every subcommand) so bad flags exit 2 — see [the root `SetFlagErrorFunc`](#the-root-setflagerrorfunc-change-3sss). |
| `tools.go` | `Tool` struct (`Name`, `Formula`, `ShellInit`, `Update`, `Description`/`Repo` added by change lst7, and `LegacyName`/`LegacyFormula` added by change 9bak — transitional rk→run-kit fields), `Roster`, `formulaPrefix`, `githubOrgBase` (`"https://github.com/sahil87/"`, change lst7 — the repo-URL base for `shll list`), `shellPlaceholder`, `tapName` (`"sahil87/tap"`, the un-suffixed tap name — distinct from `formulaPrefix`'s trailing-slash formula qualifier; added by change l6lo for the then-`brew trust --tap` ceremony, now used only by `doctor`'s tap-level trust check after change 0854 removed `--trust-tap`), the positional-arg target resolver `resolveTargets` + `shllTargetToken` (change b2vg — name-validation only, single-sourced with `Roster`, shared by `update`/`install`/`uninstall`; gained an `aliased []string` return + the `legacyAliases` map / `aliasNoticeFmt` / `printAliasNotices` helper in change 9bak; change kkaj added `uninstall` as the third consumer — `runUninstall` calls `resolveTargets(args, true)` (`allowShll=true`, like `update`), so `shll` is a legal explicit-only uninstall target processed LAST, distinct from the shll-first *display* ordering below — see [uninstall](/cli/uninstall.md#shll-self-uninstall) — see [update](/cli/update.md#positional-tool-name-args--subset-targeting-change-b2vg) and [the rk→run-kit rename](#the-rkrun-kit-rename--migration-fields-change-9bak)), and the shared shll-self display entry: `shllSelf` (the `Tool`-shaped descriptor), `shllSelfDescription` (`"the manager for the shll toolkit"`), and `shllSelfVersion()` (= `normalizeVersion(version)`), all added by change bb7r — see [the shared `shllSelf` descriptor](#the-shared-shllself-descriptor-change-bb7r). |
| `brew.go` *(extended by kkaj)* | Shared brew helpers used by the brew-coupled subcommands (`install`, `update`, `uninstall`, `doctor`): `hasBrew`, `isInstalled`, `brewBinary`, `brewMissingHint`, `installBrewMissingHint`, `uninstallBrewMissingHint` (change kkaj — the uninstall-command-specific brew-missing stderr line, kept separate so each command names itself; the update spec's verbatim `brewMissingHint` assertion is preserved), `shllFormula`. **The keg read (change 9bak):** `probeInstalledLeaf(ctx, formula) (installed, leaf, version)` is now the **sole** `brew list --formula --versions` invocation — it returns the keg leaf name alongside the install fact and version; `probeInstalledVersion`/`isInstalled`/`installedVersion` are thin wrappers over it, and `parseBrewLeaf` (first whitespace field, no regex) is the leaf extractor feeding the migration guard — see [the keg-leaf parser](#the-keg-leaf-parser-parsebrewleaf--probeinstalledleaf--brewgo). Also hosts the trust helpers: `brewTrustAvailable` (capability probe via `brew trust --help`), `brewTrustFormula` (`brew trust --formula <formula>` — per-formula trust for `shll install`, change 0854), and `brewTrustList` (`brew trust --json=v1`, JSON-decoded — the read-only trust set for `doctor`'s trust sub-check, change 0854). **Change 0854 removed** the whole-tap `shell-setup --trust-tap` ceremony helpers — `brewTrustTap`, `ensureTapTrust`, `trustHatchHint`, and `brewEnv`/`noRequireTapTrustEnv` (the 38a6 Linux workaround) are gone. `shell-init` and `version` are install-mechanism agnostic and do NOT consult brew — they detect runnable tools via `proc.ErrNotFound` from the sub-tool invocation itself. See [install §per-formula trust](/cli/install.md#per-formula-trust-before-install-change-0854), [doctor §the trust sub-check](/cli/doctor.md#the-trust-sub-check-change-0854), and [update](/cli/update.md). |
| `doctor.go` *(change d0ct; extended by bb7r, 0854, 9bak)* | `newDoctorCmd()` (registers the `--json` bool flag) + `runDoctor` + `evaluateTool` + `probeVersion` (three-way version state — since change 9bak delegates to `version.go`'s `probeToolVersion`, inheriting the legacy-name fallback rather than re-implementing it) + `resolveWiringFact` (read-only rc-block check) + `resolveTrustFact`/`trustFact` (read-only `brew trust --json=v1` trust set, change 0854 — the per-installed-tool "formula trusted?" sub-check, WARN on installed-but-untrusted) + `resolveMigrationFacts` (change 9bak — reuses `update.go`'s shared `probeRunKitMigration` gate to classify run-kit for the pending-migration/dual-rack WARNs) + the typed `doctorResult` struct (text + JSON render from one source) + marker/suggestion named constants (incl. `suggestNotTrustedFmt` change 0854, `suggestPendingMigrationFmt`/`suggestDualRackFmt` change 9bak). Change bb7r added `shllDoctorResult()` — the always-OK shll-first row built directly (not via `evaluateTool`, no self-subprocess), prepended in `runDoctor` and excluded from the problem-count denominator. Read-only diagnostic. See [doctor](/cli/doctor.md). |
| `install.go` *(extended by bb7r, 0854, 9bak)* | `newInstallCmd()` + `runInstall` + `shllSelfInstallNote` (`"shll — already present / self-managed"`, change bb7r — the prepended shll-first informational line) + the `--no-trust` flag (`noTrustFlag`/`noTrustFlagUsage`, change 0854 — opts out of the per-formula trust step that now runs by default before each install) + the `installTarget{tool, migrate}` classification struct (change 9bak — the missing-partition marks a legacy-keg run-kit `migrate:true` so it routes through the brew-direct migration action instead of a blind `brew install`, trust-then-migrate; reuses `update.go`'s `probeRunKitMigration` + `migrateRunKit`). See [install §legacy-keg routing](/cli/install.md#legacy-keg-routing-to-migration-change-9bak). |
| `update.go` *(extended by 9bak, kkaj)* | `newUpdateCmd()` + `runUpdate`; also the change-r01z "What changed:" digest (`versionBump`/`makeBump`, `printUpdateDigest`, the `digestHeader`/`digestToolIndent` literals). Change 13k3 rewrote `printUpdateDigest` to render full release bodies inline via the shared `renderReleases` helper and **removed** the `digestFullNotes` constant, the `digestSpecs` helper, and the `digestReleaseIndent` constant. Change 9bak added the rk→run-kit migration guard: the `needsMigration`/`dualRack` fields on `probeResult`, the shared `probeRunKitMigration` gate (reused by `install`/`doctor`), the brew-direct `migrateRunKit` action + its `migrationDaemonNoteFmt`/`migrationDualRackNoteFmt` constants, and the `needsMigration` branch in `upgradeArgv`/`upgradeTool` (single-source-of-truth for the dry-run preview). Change kkaj **retargeted `migrationDualRackNoteFmt`** at `shll uninstall <tool>` + the leaf-name `brew uninstall rk` (both call sites — `upgradeTool` state-C + `migrateRunKit` leftover). See [update §the rk→run-kit migration guard](/cli/update.md#the-rkrun-kit-migration-guard-change-9bak). |
| `uninstall.go` *(change kkaj)* | `newUninstallCmd()` (`Use: "uninstall [tool...]"`, `cobra.ArbitraryArgs`, `--dry-run` reusing `dryRunFlag`, and the new `--yes`/`-y` bool flag — `yesFlag`/`yesFlagShorthand`/`yesFlagUsage`) + `runUninstall(ctx, stdin io.Reader, stdout, stderr, dryRun, yes bool, args)`; the `uninstallTarget` classification struct (carrying the run-kit `runKitNewInstalled`/`runKitLegacyKeg` facts for dry-run parity); `reverseRosterOrder` (derives dependents-first from the single `Roster`, shll-self last); the `brewUninstallArgv` single-source builder threaded into BOTH the live run (`uninstallOne`) and the preview (`previewRowsFor`); the run-kit dual-name sweep `uninstallRunKit` (probe-then-act, leaf-verified) + its presence classifier `probeRunKitInstalled`; the confirmation gate (`printRemovalPlan`, `confirmProceed`) reading an injected `io.Reader` stdin via the `stdinIsTTY` seam; and the named literals (`uninstallProceedPrompt`, `uninstallAbortedMsg`, `uninstallNoTTYHint`, `uninstallPlanHeader`, `uninstallNothingMsg`, `notBrewManagedFmt`, `shllFarewellFmt`, `runKitDaemonStopHintFmt`, `shellUnwireHint`). `resolveTargets(args, true)` is the `allowShll=true` consumer — `shll uninstall shll` is legal, explicit-only, processed last. See [uninstall](/cli/uninstall.md). |
| `changelog.go` *(change r01z; extended by 13k3)* | `newChangelogCmd()` + `runChangelog` (writer-seam) + the positional `tool@old..new` spec parser (`parseChangelogSpecs`/`splitRange`, `specToolSep`/`specRangeSep` constants) + the concurrent resolver (`resolveChangelog`/`resolveOneSpec`, `resolvedChangelog`/`noteKind`) + the full-body renderer (`renderChangelogResult` + the shared `renderReleases` release-block/cap helper extracted by change 13k3 and reused by `update.go`'s digest, `changelogCapPerTool`). Reuses `resolveTargets` for name validation and [internal/changelog](/internal/changelog.md) for fetch/filter. See [changelog](/cli/changelog.md#output-shape). |
| `shell_init.go` | `newShellInitCmd()` + `runShellInit`. See [shell-init](/cli/shell-init.md). |
| `shell_setup.go` | `newShellSetupCmd()` + `runShellSetup` (renamed from `shell_install.go`/`newShellInstallCmd`/`runShellInstall` by change ri3h; `shell-install` kept as a cobra alias). See [shell-setup](/cli/shell-setup.md). |
| `version.go` *(extended by 9bak)* | `newVersionCmd()` + `runVersion`; also the shared install probe `probeToolVersion` + `toolInstalled` (extracted by change lst7, consumed by `version` and `list`). Change 9bak factored the bounded `<name> --version` invocation into `probeVersionByName` and made `probeToolVersion` retry with `tool.LegacyName` on `proc.ErrNotFound` only (the rk→run-kit display-surface fallback; also consumed by `doctor`'s `probeVersion` and by `migrateRunKit`'s unlinked-keg post-check). See [version](/cli/version.md). |
| `list.go` *(change lst7; extended by bb7r)* | `newListCmd()` + `runList`; the two output renderers (`writeListTable` aligned `tabwriter` table, `writeListJSON` bare JSON array via `json.Encoder` with `SetEscapeHTML(false)`) — both prepend the shll-first entry (change bb7r); `probeInstalled` (concurrent per-roster-tool install probe via `toolInstalled`, indexed by position, mirroring `update.go`'s `probeRoster`); `repoURL` (the single `githubOrgBase + t.Repo` composition point); `statusMarker` (color-glyph/ASCII status cell); `jsonFlag`/`jsonFlagUsage` and the status-marker constants; the `listItem` JSON struct (gained the `Self bool `json:"self,omitempty"`` field in change bb7r). See [list](/cli/list.md). |
| `standards.go` *(change vo8c; extended by i70w)* | `newStandardsCmd()` (`Use: "standards [name]"`, `cobra.MaximumNArgs(1)`, `--json` flag) + `runStandards(stdout, stderr, args, jsonOut)` seam; the `//go:embed standards/*.md` → `standardsFS embed.FS` (**this repo's first `//go:embed`**) + `//go:generate ../../../scripts/sync-standards.sh`; the hardcoded `standardsRoster []standard` slice (fields `Name`/`Description`/`Scope`/`SourcePath`/`EmbedName` — the `Scope` field + a fourth `skill` entry added by change i70w, **four entries** with `SourcePath`s under `docs/site/standards/`) mirroring `tools.go`'s `Roster`; the `standardJSONItem` `{name, description, scope, source_path}` struct (`scope` additive per format-stability rule, change i70w); the renderers `writeStandardsTable` (aligned three-column `tabwriter` name·scope·description, no status/color — a static glossary) / `writeStandardsJSON` (bare array, `SetEscapeHTML(false)`, 2-space, trailing newline) / `writeStandardDoc` (byte-identical embedded bytes → stdout; unknown name → stderr diagnostic + `errSilent`); the `standardByName`/`validStandards` roster-derived helpers; and the named constants `standardsEmbedDir`/`standardsJSONFlag`/`standardsJSONFlagUsage` (no magic strings). No subprocess — `internal/proc` untouched (Constitution I vacuous). See [standards](/cli/standards.md) (command) and [standards-content](/cli/standards-content.md) (the standards documents + `docs/site/standards/` restructure). |
| `skill.go` *(change agst)* | `newSkillCmd()` (`Use: "skill [tool]"`, `cobra.MaximumNArgs(1)`) + `runSkill(ctx, stdout, stderr, args)` seam; the `//go:embed skill/skill.md` → `skillFS embed.FS` (shll's OWN bundle, `docs/site/skill.md`, synced by the extended `scripts/sync-standards.sh`) + `//go:generate ../../../scripts/sync-standards.sh`; the glossary printer `writeSkillGlossary` (installed-only, shll-first, `tabwriter`, PATH-only `toolInstalled`, no brew, trailing hint — never concatenates bundles), the per-tool `writeSkillBundle` (legacy `rk`→`run-kit` alias via `legacyAliases`/`rosterHas`, unknown name → `errExitCode{usageExitCode}`, a Roster tool via a byte-identical `proc.RunCaptured` passthrough with not-installed/unsupported → one-line stderr + `errSilent`), and `writeShllOwnBundle` (embedded self-bundle in-process); the named constants `skillEmbedPath`/`skillSubcommand`/`skillProbeTimeout` (2s)/`skillHintLine`/`skillUnsupportedFmt`/`skillNotInstalledFmt`. The **sole `proc.RunCaptured` consumer** (see [internal/proc §RunCaptured](/internal/proc.md#runcaptured--transportcaptureall-change-agst)). See [cli/skill](/cli/skill.md). |
| `agent_setup.go` *(change agst)* | `newAgentSetupCmd()` (`Use: "agent-setup"`, `cobra.NoArgs`, `--print`/`--uninstall` bool flags) + `runAgentSetup(ctx, env, stdout, stderr, printMode, uninstallMode)` seam; the canonical `agentSkillContent` **Go string constant** (portable frontmatter `name`+`description` only; `name`==`skillDirName`==`shll-toolkit`); `skillTargetRelDirs` (the two `$HOME`-relative skill dirs `.agents/skills` + `.claude/skills`) + `resolveSkillTargets`; `runAgentPrint`/`runAgentInstall`/`runAgentUninstall`; `placeSkill` (three-state wrote/unchanged/updated per path, read-then-compare) + the local `bytesEqual`; `delegateRunKitAgentSetup` (the ONE subprocess — `run-kit agent-setup [--uninstall]` via `proc.RunForeground`, silent-skip on `ErrNotFound`; consumes `install.go`'s `runKitToolName`); named constants `agentSetupErrPrefix`/`skillDirName`/`skillFileName`/`runKitAgentSetupSub`. Plain file I/O + one delegation subprocess. See [cli/agent-setup](/cli/agent-setup.md). |
| `ui.go` *(change y630; extended by 6vuo, r01z, 13k3, kkaj)* | Shared UI helper — TTY/`NO_COLOR` detection (`colorEnabled`), the injectable **`stdinIsTTY` seam** (change kkaj — a swappable package-level `var stdinIsTTY = defaultStdinIsTTY`, mirroring the `proc.Runner`/`nowFunc` injection pattern; `defaultStdinIsTTY(r io.Reader)` is true only for a real `*os.File` terminal, mirroring `colorEnabled` but WITHOUT the `NO_COLOR` check — interactivity, not styling; consumed by `shll uninstall`'s confirmation gate so a `bytes.Buffer`/`strings.Reader` test stdin hits the non-TTY branch deterministically), the per-tool header printer (`printToolHeader(w, name, pos, total, color)` → `▸ [N/M] <tool>` / `==> [N/M] <tool>`; change 13k3 made the color form render the whole `▸ [N/M] <tool>` run as one **bold-cyan** span, see [shared UI helper](#shared-ui-helper-uigo)), the summary-tail printer (`printSummaryTail(w, succeeded, total, elapsed, color)` → appends `in <dur>` to both forms), the `formatDuration` helper (`d.Round(time.Second).String()`), the dry-run preview helpers (`previewRow` struct + `printUpdatePreview`/`printInstallPreview`/`printUninstallPreview` (change kkaj) → shared `printPreviewRows` aligned-column printer; header constants `updatePreviewHeaderFmt`/`installPreviewHeaderFmt`/`uninstallPreviewHeaderFmt` (change kkaj — `"Would uninstall %d tools:"`); `previewIndent`/`previewGap`), the shell-init comment-separator emitter (`toolComment` → `# ── <tool> ──`), the named ANSI SGR constants (`ansiReset`, `ansiBold`, `ansiBoldCyan`, `ansiGreen`, `ansiRed`), the changelog-surface glyph-degrade helpers `arrow(color)`/`dash(color)`/`more(color)` (change r01z) with their glyph/ASCII pairs (`→`/`->`, `—`/`--`, `…`/`...`), and the **`bold(color, s)` anchor helper** (change 13k3 — wraps `s` in `ansiBold` … `ansiReset` when color, else returns it unchanged; mirrors the `arrow`/`dash`/`more` glyph-helper idiom so no call site open-codes an escape; plain bold, NOT bold-cyan, so the header > anchor > body hierarchy holds). shll's OWN Unicode framing degrades to ASCII on a non-TTY/`NO_COLOR` stream per the [per-tool-output-separation spec](/cli/changelog.md#ascii-degrade-per-tool-output-separation-spec) (distinct from the spec-exempted summary-tail em-dash + shell-init box-drawing chars). Holds presentation logic only — **no** subprocess calls, **no** command of its own. Consumed by `update.go`/`install.go` (header + tail + color + preview), `changelog.go`/`update.go`'s digest (glyph + `bold` helpers), and `shell_init.go` (`toolComment` only — never the color/header path, per the [eval-safety exception](/cli/shell-init.md#the-deliberate-exception--do-not-unify-onto-the--header)). |
| `clock.go` *(change 6vuo)* | The injectable wall-clock seam — a single package-level `var nowFunc = time.Now`. Mirrors the `proc.Runner` package-level-swappable injection pattern: production uses the real `time.Now`; tests swap it via the `installFakeClock` t.Cleanup helper (`clock_test.go`) to a deterministic clock so the duration-bearing summary-tail golden strings stay exact. `runUpdate`/`runInstall` capture a start time after their nothing-to-do/dry-run short-circuits and compute elapsed immediately before the tail. No subprocess calls. |

Each command file has a paired `_test.go` (test-alongside per `code-quality.md`) — including `doctor_test.go` (change d0ct), which drives `runDoctor` with `bytes.Buffer` writers, a fake `proc.Runner`, and a `t.TempDir()` rc file via a map-backed env; `ui.go` has `ui_test.go`, which unit-tests the helpers directly (`bytes.Buffer` writers naturally hit the plain-ASCII branch).

## Shared UI helper (`ui.go`)

Three commands frame their per-tool output via the shared `ui.go` helper (change y630), so the TTY/`NO_COLOR`/glyph logic lives in exactly one place:

- [cli/update](/cli/update.md#per-tool-output-separation-change-y630) — per-tool `▸ [N/M]`/`==> [N/M]` header (including `shll (self)`) + summary tail + the `--dry-run` preview.
- [cli/install](/cli/install.md#per-tool-output-separation-change-y630) — mirrors update's header + tail + preview.
- [cli/shell-init](/cli/shell-init.md#shell-comment-separator-change-y630) — `# ── <tool> ──` comment separator only; deliberately **not** the header/color path (eval-safety).

`golang.org/x/term` (TTY detection) is the codebase's only color/terminal dependency, added by change y630 — it is the codebase's first terminal inspection. `version` is deliberately untouched (its lines already self-label).

### Output polish + `--dry-run` (change 6vuo)

Change 6vuo extended `ui.go` (and added `clock.go`) without adding any subcommand or dependency, applying four additive features to **both** `update` and `install`:

1. **`[N/M]` progress counters** in the per-tool header — `printToolHeader` gained `pos, total int` parameters. For `update`, `M = installed-roster-count + (1 if shll is brew-installed)`, computed up front, with `shll (self)` as `[1/M]` and first; for `install`, `M = len(missing)`.
2. **Section spacing** — a blank line before each per-tool header except the first, and a blank line before the summary tail (emitted by the commands, not the helper). Empty/short-circuit cases emit none.
3. **Run duration in the summary tail** — `printSummaryTail` gained an `elapsed time.Duration` parameter, appending `in <dur>` to both the success and partial-failure forms (duration before the em-dash in the partial form). Measured via the `nowFunc` clock seam (`clock.go`). Duration is a run fact, not an outcome claim — the honesty constraint is preserved.
4. **`--dry-run` flag on `update` and `install`** — a cobra bool flag (`dryRunFlag`/`dryRunFlagUsage` named constants in `update.go`, shared by both commands; threaded as a `dryRun bool` parameter into `runUpdate`/`runInstall`). When set, the read-only probes still run but **no write** is performed; the command prints an aligned-column preview (`printUpdatePreview`/`printInstallPreview`) of the exact per-tool commands and exits 0. This is a **flag on existing commands, NOT a new subcommand** — Constitution VII's "could this be a flag?" test is satisfied, so the surface-area count was unchanged by change 6vuo (five at that time; six since change lst7 added `list`) (recorded per the code-review rule). See [cli/update §`--dry-run`](/cli/update.md#dry-run-change-6vuo) and [cli/install §`--dry-run`](/cli/install.md#dry-run-change-6vuo).

The `update` dry-run preview renders each tool's command from the **shared `upgradeArgv`** (`update.go`) — the single source of truth `upgradeTool` uses for the live run, so the preview can never drift from what the run would do — joined to a display string via `argvString`. `ui.go` stays presentation-only: it receives already-built `[]previewRow` and only aligns/prints them, keeping all subprocess knowledge in the command files (Constitution I).

### Bold-cyan header run + the `bold` anchor helper (change 13k3)

Change 13k3 extended `ui.go` with two presentation-only tweaks (no new subcommand, no new dependency), both TTY-gated via the existing threaded `color` decision:

1. **Whole-run bold-cyan header.** `printToolHeader`'s color branch now renders the entire `▸ [N/M] <tool>` run inside a **single bold-cyan span** (`ansiBoldCyan` … `ansiReset`) — previously only the `▸` was bold-cyan and the name bold-default. Because all three surfaces share this function, the change covers `shll update`, `shll install`, and `shll changelog` uniformly. The plain branch (`==> [N/M] <tool>`, non-TTY / `NO_COLOR`) stays **byte-identical**. Pinned by `TestPrintToolHeader_ColorForm` (bold-cyan run) / `TestPrintToolHeader_PlainForm` (unchanged).
2. **`bold(color, s)` anchor helper.** A new helper wraps `s` in `ansiBold` … `ansiReset` when `color`, else returns `s` unchanged — the single place the changelog-surface anchor styling (the digest / `shll changelog` transition lines and the release tag/title lines) is applied, so no call site open-codes an escape (code-quality.md: named constants, no magic strings). It mirrors the `arrow`/`dash`/`more` glyph-helper idiom. Deliberately **plain bold, NOT bold-cyan** — bold-cyan stays reserved for `printToolHeader` so the visual hierarchy header > anchor > body is preserved. Consumed by `renderChangelogResult`/`renderReleases` (`changelog.go`) and `printUpdateDigest` (`update.go`). See [cli/update §digest rendering](/cli/update.md#digest-rendering-printupdatedigest) and [cli/changelog §output shape](/cli/changelog.md#output-shape).

## Cross-references

- Constitution I (Security First) → all subprocesses go through [`internal/proc`](/internal/proc.md); network I/O is likewise isolated in [`internal/changelog`](/internal/changelog.md) (the ONLY `net/http` importer — no `cmd/shll` file imports it, guarded by `TestCmdShllNoNetHTTP`, change r01z). (`ui.go` makes no subprocess calls — it is pure presentation.)
- Constitution III (Wrap, Don't Reinvent) + IV (Composition, Not Replacement) → every subcommand shells out; nothing reimplements brew or per-tool logic. shll's framing prints only *around* each subprocess; sub-tool bytes are never rewritten.
- Constitution V (Graceful Degradation) → uninstalled tools never produce errors; missing tools are skipped silently. Eval-safety drives `shell-init`'s comment-separator exception (see [cli/shell-init](/cli/shell-init.md#the-deliberate-exception--do-not-unify-onto-the--header)).
- Constitution VII (Minimal Surface Area) → the user-facing subcommand list is twelve (`doctor`, `install`, `update`, `uninstall`, `changelog`, `shell-init`, `shell-setup` (formerly `shell-install`, kept as an alias), `version`, `list`, `standards`, `skill`, `agent-setup`); change agst added `skill` (the runtime per-tool-bundle composer — the `shell-init` composition pattern, cannot be a flag since none reads/composes agent bundles; see [cli/skill](/cli/skill.md)) and `agent-setup` (cross-toolkit machine wiring graduated from the mis-homed `run-kit agent-setup`, a distinct machine-provisioning verb; see [cli/agent-setup](/cli/agent-setup.md)) as the eleventh and twelfth; change vo8c added `standards` as the tenth (justified above — an agent-facing reader for the toolkit's binding standards; cannot be a flag on an existing subcommand — none reads or prints documents — and belongs to no per-tool CLI since the standards govern all seven tools; see [cli/standards](/cli/standards.md)); change kkaj added `uninstall` as the ninth (justified above — a distinct destructive verb providing the clean-slate repair path, cannot be a flag on `install`, cannot live in a per-tool CLI; see [cli/uninstall](/cli/uninstall.md)); change r01z added `changelog` as the eighth (justified above — deterministic post-hoc release-notes re-display, cannot be a flag on `update`; see [cli/changelog](/cli/changelog.md#constitution-vii-justification)); change y630 added behavior to existing commands and a non-command helper file (`ui.go`), not a new subcommand; change ri3h renamed `shell-install` → `shell-setup` (alias preserves the old name) without changing the count; change 6vuo added the `--dry-run` flag to `update`/`install` and the `clock.go` seam — flags and a helper file, not a new subcommand; change lst7 added `list` (with its own Constitution VII justification — see the [per-subcommand justification](#constitution-vii-justification-per-subcommand) and [cli/list](/cli/list.md#constitution-vii-justification)); change d0ct added `doctor`, justified as a read-only cross-tool diagnostic — its `--json` is a flag on `doctor`, not a separate subcommand (see [cli/doctor](/cli/doctor.md)); change bb7r added *behavior* to existing commands (the shll-first row/object/line in `list`/`doctor`/`install`) and no new subcommand — its only recorded decision is the lst7 "no self-row" reversal (display, not constitutional) — see [the shared `shllSelf` descriptor](#the-shared-shllself-descriptor-change-bb7r).
- Constitution III (Tool Roster Source of Truth) → `shll` is deliberately NOT in `Roster` (the managed sub-tool list); the shll-first display entry is the prepended shared `shllSelf` descriptor, so `len(Roster)` stays 6 and the leaves-first invariant is untouched — see [the shared `shllSelf` descriptor](#the-shared-shllself-descriptor-change-bb7r).
